1. PARTIALLY HELD — modern clipboard, backup, cloud, and share-link transfers preserve both tiers, but legacy bare-array account restores still inherit the outgoing tab’s tiers.

2. HELD — `repairPartDisposal` is reached through `initFactories` on the load paths inspected, safely handles JSON-representable malformed maps, and deleting properties while iterating the snapshot returned by `Object.entries` is safe.

3. NOT HELD — capping the Depot statistic does not stop the sink statistic from reporting those same items too.

DO NOT SHIP

1. A sunk-and-depoted surplus is still double-reported. [statistics.ts](/tmp/claude-1000/-home-matt-code-satisfactory-factories/643f289c-110a-4a34-a48b-6f79719f1039/scratchpad/rv2/web/src/utils/statistics.ts:339) now reports `min(spare, containers × uploadRatePerMin)` as Depot throughput, but the calculation engine continues assigning the entire pre-sink surplus to `amountRequiredSink`. For a 100/min surplus and one tier-0 Uploader, the UI therefore reports 15/min to the Depot and 100/min sunk: 115/min of destinations from 100/min of material. Multiple factories preserve the same error independently because each source is capped and then summed. Passing another rate consistently changes the cap and displayed capacity, but does not fix conservation. The current test only asserts the Depot’s 15/min figure and never asserts that Depot plus sink throughput equals the available surplus. Reduce the reported sink throughput by the Depot allocation—or introduce distinct ledger and physical-throughput fields—and add conservation tests for one and several contributing factories at multiple supplied rates.

2. Legacy account restores retain Depot research from the plan being replaced. In the array branch of [app-store.ts](/tmp/claude-1000/-home-matt-code-satisfactory-factories/643f289c-110a-4a34-a48b-6f79719f1039/scratchpad/rv2/web/src/stores/app-store.ts:828), `loadServerPlan` clears only `plannerVersion`; it does not clear `depotUploadTier` or `depotExpansionTier`. Thus restoring a pre-tier bare-array account plan into a tab set to tier 0 makes that legacy plan read as tier 0, although absence is defined as fully researched. This silently changes capacity warnings and research costs according to unrelated outgoing state. Clear both fields in the legacy-array restore branch, matching legacy clipboard paste, and test restoration over a tab with non-default tiers.