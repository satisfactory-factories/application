# Changelog

All notable changes to this project are documented in this file. It mirrors the structure of the in-app [Change Log](https://satisfactory-factories.app/changelog) — same sections, full technical detail. For the release history prior to Alpha v0.4 (the 0.1.x–0.3.x scaffolding releases), see the [GitHub commit history](https://github.com/satisfactory-factories/application/commits/main).

## Beta v0.7

_In development._

### Search the plan

- **A search box sits next to Options in the tab bar.** Type a factory name to jump straight to it,
  or type a part to see every factory that touches it. Results appear under the box as you type.
  Ctrl/Cmd+K opens it from anywhere; the arrow keys walk the results and Enter opens one. On a
  narrow screen the box is a search button that opens the same panel. Closes #611.
- **Part results are grouped by what the factory does with the part**: Production first, then
  Byproduct, then Other usage — imports, exports and plain ingredient demand, each row saying which
  it is and how much per minute.
- **Clicking a result lands on the row it names**, not just the factory card: the product row for
  production and byproducts, the import row it comes from for an import, and the part's satisfaction
  row for everything else.
- **Every result wears the factory chip** the rest of the planner uses, and a factory in a group
  carries that group's colour on the chip's left edge.
- The plan is indexed once per change rather than re-scanned per keystroke, so a large plan stays
  responsive while typing.

### Custom buildings

- **Add any building that makes nothing to a factory**, under Products and Power Generators: portals, train stations, freight platforms, truck stations, drone ports, radar towers, the AWESOME Sink, hypertube entrances, jump pads, pipeline pumps and lights. Twenty buildings in all. They count towards the factory's power draw and its building list.
- **The Main Portal's Singularity Cells are a real demand.** Two a minute per portal, satisfied by importing them like any other part, so a portal room short of cells reads as a shortage.
- **The Demo plan has a Portal Hub**: ten Main Portals, 2.5 GW, and 20 Singularity Cells a minute imported from a new Singularity Cells factory.
- Satisfaction breakdowns list custom building upkeep separately from what recipes consume.

### Material costs

- **Power & Buildings now has a Material Costs panel**, showing what it would cost, in parts, to build every production building, power generator and custom building the factory needs. Closed by default; toggle it open to see the breakdown.
- Each part lists its total quantity, and a chip per building that needs it — the building's image and how many of them.
- **Use this as a guide only.** No assumptions are made about belts, foundations, or any other structural or cosmetic building — only production buildings, power generators and custom buildings directly involved in making your products are counted.

### Sidebar: arrange the plan without dragging

- **The sidebar can be scrolled on a phone again.** Picking a factory or a group up is the same gesture as scrolling the list, so touching a row to scroll dragged it instead. Drag is now offered only where the pointer is precise enough for it; the new **Arrange** dialog does the same job with buttons.
- **An "Arrange" button opens a dialog for reordering the plan.** Groups against each other, factories within their group, and factories from one group into another, all with buttons. Groups can still be dragged there too, where there is a pointer precise enough to drag with.
- **Add Factory moved to the top of the sidebar.** It used to sit below every group, so a plan with a lot of groups meant scrolling all the way down to add one more ungrouped factory — which then appeared back at the top. (#597)
- **The sidebar now follows the orange scroll-spy indicator.** Scroll the plan and the sidebar smoothly scrolls itself to keep the highlighted factory in view, instead of leaving it to drift off-screen. Closes #598.

### AWESOME Sinks and the Dimensional Depot

Closes #498 and the surplus half of #7. Every item under a factory's Satisfaction now has a
**Storage** column, left of Satisfaction, holding two counts: how many AWESOME Sinks and how many
Dimensional Depot Uploaders you have put on that item's surplus.

- **An AWESOME Sink disposes of the surplus, so the planner treats it as gone.** Set one or more on
  an item and its surplus reads `0/min surplus` with a gold **n/min sunk** chip beside it and the
  pre-sink figure in brackets underneath — the number sinking removed is never hidden. The sink is a
  priority splitter, not a consumer with an appetite of its own: internal use and exports are served
  first and always win, so adding an export request later shrinks the sunk amount by itself. A fully
  sunk item also stops being nagged with **Trim**, because a factory that deliberately overproduces
  into a sink is doing exactly what it was built for.
- **The first sink and the first Uploader each get a one-off explainer.** Per browser, shown once.
  The sink one states the two assumptions behind a sunk item: Programmable Splitters sending the
  excess to your sinks, and a belt of adequate speed feeding them. Break either and the numbers are
  inaccurate. The Uploader one points at the plan-wide Dimensional Depot summary at the top of the
  planner, and is clear that nothing is assumed about how much an Uploader takes off the belt, only
  that it eventually backs up, and takes you straight to the summary. The Dimensional Depot
  section returns the favour with a button to the Mercer Sphere statistics, since that is where its
  costs are counted. The upload and expansion research levels can be set from either place, and
  are the same setting: change one and the other follows.
- **Sinks draw power.** 30 MW each, counted into the factory's consumption and so into the plan's,
  matching `Build_ResourceSink_C` in the game's own data. The Depot Uploader draws nothing.
- **The sink refuses what the game refuses.** No control is offered for fluids (the sink has a
  conveyor input only) or radioactive items, and a count left on one from an earlier edit stays
  inert rather than quietly zeroing a surplus that in game would not move.
- **Both controls are offered on every item**, whether the factory makes it, imports it, or is
  short of it. They first shipped gated on a surplus, which hid them from the build the Depot is
  most useful for: a logistics factory that imports a part precisely so it can upload it has
  imports that balance exactly, so it had no surplus, so it was offered no Uploader. Nothing needed
  the gate — a sink takes what is spare and nothing more, so one set on a part with nothing spare
  is inert rather than forbidden, and starts working by itself when the part does have something
  spare. What the buildings physically refuse (fluids, and radioactive items for the sink) is still
  enforced.
- **A Dimensional Depot Uploader deliberately changes no number.** The Depot is finite storage: it
  fills, and then it backs up like any other container. Marking an item for the Depot records what
  you are building and what it costs, and leaves the surplus exactly as it is.
- **New "Will cause backlog" warning.** Any item left with a surplus nothing consumes, nothing
  exports and no sink takes will fill the belt and stall the buildings making it — including the
  case nothing could previously see, where a factory makes 200 Iron Plates, ships 100, and the other
  100 quietly back up. It is an amber warning and marks the factory amber, because there is now a
  control in the same row that fixes it; it can be switched off entirely under
  **Options → Satisfaction** for a plan mid-build.
- **New Dimensional Depot section**, its own card under the Statistics summary and only on plans
  that use it. One row per item: what the plan has spare to upload, how many Uploaders are on it,
  and a pill per factory carrying that factory's own count. Flags an item arriving faster than its
  Uploaders can take it (240/min each, fully researched), so the remainder still backs up.
- **Mercer Spheres join Power Shards and Somersloops** in the statistics, at one per Dimensional
  Depot Uploader, read off the game's build recipe, and the MAM research the Depot costs is listed
  under them with a tick-box each: upload research (the two unlock nodes plus every upload upgrade
  up to the plan's tier), depot expansion, and the optional Manual Uploader. All three are off the
  total by default, since they are paid once per save rather than once per plan. Ticked together at
  full research they come to the 97 the wiki quotes for the whole chain, which a test now pins.
  Mercer Spheres are counted in one place only, so the Depot section and its sidebar entry no
  longer repeat the figure. The Mercer Sphere icon is new —
  it is a collectable rather than a craftable part, so it was never in the icon set the planner
  shipped with.
- **The Depot's MAM upload research is part of the plan.** An Uploader moves 15/min unresearched and
  doubles with each of the four upgrades to 240/min, and the rate is per Uploader — two on one item
  fill at 480/min, though depot storage does not stack. The section carries a research selector and
  reports each item's rate against what its own Uploaders can take (`40 / 480/min`), flagging any
  that cannot keep up. Saved on the plan, so a shared plan carries the world it was written against.
- **The Dimensional Depot has a sidebar entry**, beneath the Global Factories Summary, carrying
  icon-only counts of items tracked, Uploaders and Mercer Spheres, plus an over-capacity warning
  when it applies.
- **The Dimensional Depot carries the expansion research too**, beside the upload tier: how many
  stacks of each item the Depot holds, 1 to 5. It changes no calculation, because the planner tracks
  rates rather than how full a container is, but it is the other half of what the MAM sells for the
  Depot and it is what "the Depot is finite" actually means. Saved on the plan like the upload tier.
- The Dimensional Depot's table gives its three number columns fixed widths and lets the factory
  pills — the column that actually grows with the plan — take the rest. The item column is sized to
  the longest name in the game, so nothing in it wraps.
- Clicking a factory pill in the Dimensional Depot lands on that item's own row under the
  factory's Satisfaction, unhiding the card and opening its group on the way, rather than dropping
  you at the top of the factory and leaving you to find the item.
- **The Demo plan uses the Depot**, so the section is visible on the first plan anyone opens:
  two Uploaders on Copper Ingot, and one each on Circuit Board, Copper Sheet and Plastic. Only the
  Copper Ingot line has a steady surplus to give, so the other three read `0`, with a tooltip
  explaining that an Uploader still fills off a splitter on a line that is fully spoken for.
- The Uploader also takes a conveyor and nothing else, so fluids are excluded from it too. It has no
  objection to radioactive items, though, unlike the sink: uploading one is how you stop it
  irradiating you, so Uranium and Plutonium Waste can be depoted even though they cannot be sunk.
- Three tooltips that promised sinking was "coming in a future update" now point at the control that
  does it, and the **End product** chip no longer claims the planner assumes you sink it — you say so.

### Checklist: a desynced item now says what changed

A ticked checklist item whose number the plan has since moved was flagged only as "desynced",
which said something had changed but not what, so the only way to find out was to remember what the
number used to be.

- **Every desynced row now carries an amber chip with both numbers** — `560/min → 720/min` for a
  product, import or export, `4 buildings → 6 buildings` for a power generator. Hovering it spells out the
  two ways forward: build the difference and confirm it, or change the plan back to match what you
  have already built.
- **Clicking that chip confirms the new number**, exactly as re-ticking the row's checkbox does.
- **The Checklist panel opens with an amber summary** when anything has drifted, saying how many
  items are affected, with a **Reconfirm all** button for when you have already rebuilt the lot.
  Reconfirming only touches rows that actually moved — an item you never ticked stays unticked.
- **The factory's Checklist chip counts them** — `Checklist: 12/14 · 2 to reconfirm` — instead of
  saying `(desynced)`, so a collapsed factory card tells you whether it is one stale row or nine.
  The sidebar's checklist tooltip counts them the same way.
- **A desynced checklist is now a factory status of its own**, so it wears an amber **Checklist
  desync** chip everywhere the other statuses appear — the card header, the sidebar entry, the
  Factories Summary and the plan- and group-level tallies — carrying the icons of the items that
  moved, and turning the factory amber like any other warning. Previously the only clue in the
  sidebar was a counter quietly changing colour, which said a factory was amber without saying why.
- **The sidebar's checklist counter takes a chip's outline when it is amber**, rather than only
  changing colour, so it reads as a state to act on rather than a recoloured icon.
- **The checkboxes on the Products, Imports, Power and Satisfaction rows** carry the same two
  numbers in their hover text, where there is no room for a chip.

### Interface

- **Every dialog in the planner now has the same header and spacing.** Vuetify's stock card title sat the heading flush in the top-left corner with no breathing room, and each dialog had drifted its own way from there: some closed from a button at the bottom of the actions row, some had no way out but the scrim, and body text came in three different sizes. They now share one shell — a padded title row with the icon beside the heading, the close button in the top-right corner where a dialog's way out belongs, and body text at a single size. Dialogs that ask for a decision before they will go away (the out-of-sync prompt, the update-required notice) still have no corner close, deliberately.
- **The "Show Info" toggle is gone**, along with the explanatory paragraphs it hid throughout the planner. Nobody was clicking it, and the copy behind it hadn't kept pace with the app for several updates. The always-visible ⓘ tooltips elsewhere in the UI (Game Sync, upkeep, variable power, and so on) are unaffected.
- **Statistics and the Global Factories Summary now start collapsed.** A fresh visitor, or anyone opening a demo plan, used to land on a page-length wall of stats above the factory cards themselves. Item production, power shards, raw resources and building summaries within Statistics now start collapsed too, matching the per-factory power breakdown, which already did. Each section remembers your choice once you toggle it.

### Fixes

- **Enter accepts a task you are editing** instead of dropping a newline into it. Task titles are
  edited in an auto-growing textarea, so pressing enter grew the row and left the edit sitting
  there uncommitted, while the new-task field right above it has always taken enter as "add this".
  Enter now commits the edit and leaves the field, the same as clicking away; shift+enter still
  types a second line for anyone who wants one.

- **Checklist mode: ticks on the Products, Imports and export-chip checkboxes are reliable again**
  (#592, #593). The export tick sat inside the chip's own clickable area, so the chip's click
  handler and ripple layer could win the click before it ever reached the checkbox; it's now a
  sibling of the chip instead, matching how the Checklist card above it already builds the same
  control. Separately, all three checkboxes could lose a race against the browser's own
  "revert to pre-click state" step when a click was cancelled — the state change landed, but the
  box itself (and anywhere else that same tick is drawn) could stay visually unticked. Every
  checklist checkbox now keys its `<input>` on the checked value itself, which sidesteps that race.

- **Fix Product no longer ignores what the factory imports** (#595). The shortfall it wrote counted
  only what the factory produced itself, so a part with 1425/min imported against 2740/min needed
  was fixed to 2740 rather than 1315 — and the figure named on the Satisfy and Trim buttons was
  wrong in the same way, since both read from the same calculation. Where a part is imported as
  well as made on site, local production only has to cover what the imports don't: a factory
  needing 5232/min with 2100/min arriving from two other factories now offers to trim its own
  production to 3132. That button is labelled **Trim to import shortfall** rather than just Trim,
  with a tooltip saying why, since it deliberately stops short of the full requirement. Trim can
  no longer name a negative quantity either: where the imports cover the whole demand on their
  own, it now offers 0 — stop making it — rather than a number the Qty field can't hold.

- **A factory that consumes its own output no longer reports a phantom surplus to export** (#540).
  Export supply was read gross, so a mine extracting 480 ore a minute and smelting every bit of it
  on site still offered 240 of it to another factory and called the request satisfied. The figure
  is now what the factory actually has spare once production, power, buildings and sinking have
  taken their share, so an over-committed factory reads as short — and the plan-wide "produced
  items difference" in Statistics no longer counts ore that was never really available.
- **An over-committed mine can be given imports again** (#541). A mine shipping out more than it
  extracts matched the "nothing here could ever import anything" shortcut — extraction takes no
  ingredients, so its Add Import button was disabled — even with another mine in the plan able to
  cover the difference. Export demand now counts, so the button is offered. Together with #540 the
  two halves are coherent: the mine goes red and the fix for it is available in the same place.

- **The Share button now shares the plan you are actually looking at** (#535). It took its copy of
  the open tab once, when the page loaded, and the button never remounts — so after switching tabs
  it went on sharing the plan that had been open at load, under that tab's name.

- **Two power generators in one factory can no longer be issued the same ID** (#546). Generator IDs
  were drawn at random with nothing checking whether the factory already held that number, and
  those IDs key the Game Sync snapshots — so a collision (roughly one factory in 200 with ten
  generators) made the factory drop out of sync the moment it was marked as built, permanently and
  with nothing on screen explaining why. Both the generator itself and the ID backfill that runs on
  plans from before generators had IDs now issue them against what the factory already holds.

- **The Raw Resources Wizard's backup no longer zeroes an older power target** (#536). If you set a
  power target before targets became per-plan, the backup recorded it as 0 — and since the backup
  is the only undo for a migration that can't be reversed, and pasting it writes the target back
  onto the plan, restoring stamped that 0 in for good. It now reads the target the same way Copy
  plan and the rest of the planner do, legacy fallback included.

## Beta v0.6 - The "Groundwork" Update

_Released 19 August 2026._

Raw resources are no longer assumed. Ore, water, oil and gas are dug up by buildings you place, and planned and exported like anything else. Factory groups, factory icons and status chips arrive to keep a bigger plan in order.

### Mining: extraction is now part of the plan

- **The Demo plan mines every way there is**, and its Uranium arrives two ways at once: half dug on site in Uranium Power, half shipped in from a dedicated **Uranium Mine**, so the choice the planner now leaves you is visible in the first plan anyone opens. A dedicated Copper Mine feeds the Copper Ingots factory: a Mk.3 on a normal node alongside a Mk.1 on a pure one, digging 360/min against the 320 the smelters need, because you take the nodes you are given. Oil Processing extracts its own crude on site, 3 Oil Extractors on pure nodes plus 2 on normal, landing exactly on the 960/min the Plastic line drinks. A new **Raw Materials Mine** hosts two resources at once for the nuclear chain (Limestone and Sulfur), and Uranium Power pumps its own water from 22 Water Extractors. The Sulfur and Uranium groups over-produce on purpose: extractor rates are all multiples of 30, so the 160 and 200 those lines want can't be hit exactly without clocking a miner down. What the plan is still short of is deliberate and manufactured: the Copper Basics bottleneck, and the Stators, High-Speed Connectors and Encased Beams that Uranium Power has no supplier for.
- **Satisfaction chips describe a part independently now.** Extraction, importing and hand-gathering are separate facts about an item, not one label competing for the same slot, which is what let a partially-mined resource claim to be fully satisfied.
- **A mine's Imports section says why it has none.** Extraction takes no ingredients, so a mine has nothing to import. It used to report "there are no factories available to import the current product selection", which blames the rest of the plan for something simply not applicable.
- **The breaking change is announced once**, however your plan arrives (pasted, opened from a share link, restored from your account or loaded from disk), with the wizard offered as the way out of it. Once dismissed it never returns.
- **A "Mining" template**, showing the lot end to end: an Iron Mine mixing Mk.3 miners on pure nodes with a Mk.2 on a normal one, a Nitrogen resource well with its satellite spread, and a Nitric Acid factory extracting its own water on site.
- **Resource wells are supported**, for Water, Crude Oil and Nitrogen Gas. A well is one building group: the pressurizer it counts, and how many satellite extractors sit on impure, normal and pure micro-nodes. The pressurizer's clock scales every satellite at once, exactly as in game, so a well of 1 normal + 6 pure satellites reads 780 m³/min at 150 MW, and 1,950 m³/min at 503.7 MW when clocked to 250%. Satellites are counted as buildings you must place but draw no power: the pressurizer pays for all of them. **Nitrogen Gas becomes extractable for the first time**, since wells are its only source.
- **Raw resources can be extracted as products.** Pick Iron Ore, Raw Quartz, Water, Crude Oil or any other node resource as a product and the planner offers its extractor as the recipe: Miner Mk.1/2/3 for the ten solid node resources, the Oil Extractor for Crude Oil, the Water Extractor for Water. Build a dedicated mine factory and export the ore anywhere in your plan, or mine on site and smelt it in the same factory. Both work, and the ore stops being an unspoken assumption either way.
- **Miner mark and node purity are set per building group**, because one ore line routinely mixes a Mk.3 on a pure node with a Mk.2 on a normal one. Each group picks its own extractor and purity, and the two multiply with the group's clock exactly as the game does: a Miner Mk.2 on a pure node reads 240/min at 100%, 560/min at 233.3333% and 600/min at 250%, drawing 15 MW, 46 MW and 50.4 MW respectively. Purity changes the yield, never the power.
- The Water Extractor is not a special case: water sources have no purity, so it is a plain producing building at a flat 120 m³/min that happens to output a raw resource, and it overclocks like everything else (300/min at 250%).
- Buildings are tallied per extractor, so a factory mixing marks reports "Miner Mk.3 × 2, Miner Mk.2 × 1" rather than one meaningless number, and the power figures follow each group's own extractor.
- **A mine's building groups report output, not effective buildings.** Effective buildings are counted in reference-extractor units (a Miner Mk.1 on a normal node), so a mine targeting 5,760 Limestone with a single Mk.3 on a pure node read "Effective Buildings: 1.00 | 88.00 short". That is 88 Mk.1-equivalents, which means nothing when you're building Mk.3s. It now reads "Effective Output: 480/min | 5280/min short", in the item you're actually digging up. (The two figures were also being computed on different bases, so the 1.00 and the 88.00 didn't even agree with each other.)
- **When a mine is short, it says what would cover it.** A second row under the status line lists, per node purity, how many of each miner mark would close the gap, e.g. 5,280/min short shows `Impure: Mk.1: 176 | Mk.2: 88 | Mk.3: 44`, `Normal: Mk.1: 88 | Mk.2: 44 | Mk.3: 22`, `Pure: Mk.1: 44 | Mk.2: 22 | Mk.3: 11`. All three purities are shown so it never has to assume which nodes you've got. Counts are buildings in the same sense as the rest of the row (one building at 100%), so a gap a quarter of an extractor wide reads `0.25` rather than being rounded up to a whole one that would overshoot it. Resources with a single extractor don't name it, since the building group above already has.
- The building group status line is now a single flex row with uniform spacing, so the figures, the over/under chip and the sync control all sit on the same vertical centre instead of drifting off the text baseline.
- **Mines start with building group sync disabled.** Nobody actually builds Mk.1 miners, so the first thing you do to a new mine is swap the default mark, and with sync on that would write the group's new output straight over the quantity you just typed. The group is still solved into whole miners when the product is created; from then on the quantity is yours and the group imbalance is shown rather than silently applied. Water extractors are excluded and keep sync on like any other building: there is no mark to swap and no purity to set, so there is nothing to type over. The same applies to what the building count means: a water product gets the ordinary editable count, reading "Requires: Water Extractor: 22", where a mine gets the marks-and-purity treatment it needs.
- **Each raw resource in the Imports section now has a "mine it here" button**: the same box-and-plus as Add Product, sat inside the resource's pill. One click adds the extraction as a product at exactly the amount the factory is short of, on Mk.1 miners at normal purity with the building count solved for you; adjust the marks and purities per building group from there. Resources with no extractor (collectables, and the resource-well gases) don't offer it.
- **The Converter's ore recipes now name the conversion**: "Iron Ore (Convert: Limestone)" rather than "Iron Ore (Limestone)", which otherwise sat next to "Iron Ore (Miner)" in the recipe selector reading as though the ore simply came from limestone. Applies to all seventeen of them.
- SAM is now selectable as a product, since it finally has a recipe that produces it.

### BREAKING: raw resources are no longer assumed to be supplied

- **Raw resources must now be mined or imported, like anything else.** Raw demand used to be topped up silently, so a plan could never tell you it was short of ore, and a mine exporting more than its miners produce still read as green. **Plans built before this will show factories in red.** Nothing in your plan has been changed or lost. It is only being honest about what was already missing.
- **There is no setting, and that is deliberate.** An optional assumption meant the same plan could mean different things to different people, with nothing on screen to say so, and every feature downstream of raw supply had to ask which mode it was in. The bug that ended it: a factory mining 100 of the 180 limestone it needed read as *fully satisfied*, because the chip that would have mentioned the assumed 80 was suppressed by the one saying "extracted".
- **The one exception is decided by the game, not by you.** Eleven raw resources have no extractor of any kind: Leaves, Wood, Mycelia, the four alien remains, the three power slugs and the FICSMAS Gift. Demanding you mine those would leave every Fabric, Charcoal, Biomass and Protein factory permanently red with nothing the planner could offer, so they stay satisfied and wear an amber **Manually gathered** chip. The factory itself stays green: there is nothing to build and nothing to fix.
- An unmet raw resource is now a genuine shortage: a red factory, an ordinary **Shortage**, and the usual **+ Product** and **Add to factory** buttons so you can mine it on the spot or import it from a mine factory. A factory whose demand is entirely raw can now import, which it previously refused to do. It was briefly a **Raw shortage** of its own during development; being told which kind of shortage it was never told anyone anything they could act on, so ore and plate are counted and chipped the same way.

### A wizard to migrate your plans in one pass

- **The Raw Resources Wizard** lists every factory short of a raw resource and offers, per row: create a shared mine factory, mine it here, import it from a factory that already mines it, or leave it alone. It lives in **Options** and isn't only for migrating: run it any time a new factory comes up short.
- **One mine factory per resource for the whole plan**, sized to everything that asked for it, rather than one per row: a plan short of iron in eight places gets one Iron Ore Mine, not eight. New mines are built on Miner Mk.2s at normal purity, with the building count solved to match; only the count and the power depend on that guess, never the amount of ore, so adjust the marks and purities per group to match the nodes you actually have.
- **Water defaults to being extracted on site**, since that is how it is nearly always built, and a row whose resource is already mined somewhere in the plan defaults to importing from there rather than building a second mine beside the first.
- **Resource wells are the one thing it won't build for you.** A well's rate comes from its satellite nodes, so sizing one automatically multiplies the *pressurizer* instead and lands an order of magnitude out while reading as a solved plan. Nitrogen Gas rows therefore offer nothing at all, and say why. Wiring an import from a well you already have is one click in Imports, and offering that as the single option on a row that otherwise reads "not possible" makes it look as though the wizard half-solved it.
- **It shows you exactly what it is about to do and writes nothing until you confirm**: every factory it touches, everything that factory will produce, and every export it gains with the destination and the rate. There is no undo in the planner, so the whole change is built and checked off to one side first: if any part of it can't be applied, nothing is written at all rather than leaving half a plan behind.
- **Download a backup before you apply**: one button on the confirmation screen saves the plan exactly as it stands, in the same format **Paste plan** reads, so there is a way back from a run you didn't want.
- **You choose where new mines land**: the top of the plan or the bottom, defaulting to the top. Eleven new factories appended to the end of a long plan are eleven factories you have to go looking for. You can also rename any of them on that screen before they are built, and every reference to the renamed factory follows.

### Factory groups

- A plan of thirty factories was a flat list. The only ways to organise it were the name and the drag order, and that order was one long sequence with nothing marking where one concern ended and the next began. Factories now belong to a **group**: a named, coloured folder that collapses like a toggle, reorders, and can be dragged in and out of. Anything in no group falls into **Ungrouped**, pinned to the top.
- **In the sidebar**, each group is a header with its colour swatch, an editable name, a factory count, a delete button, and a second line showing what the group makes: as many item icons as the sidebar is currently wide enough for, then a `+N` tile whose tooltip lists the rest. Widen or narrow the sidebar and the row re-fits as you drag, always filling exactly one line. Drag a factory between groups, reorder factories inside one, or drag the groups themselves. A group with no rows to aim at, collapsed or simply empty, shows a dashed drop strip so it can still be dropped into.
- **A group's product row says whether it is keeping up.** Under each item icon sits that group's surplus or shortfall of it: the whole group's, netted across every factory in it, so a mine's spare ore and a smelter's shortage of the same ore cancel instead of reading as two separate problems. Green for surplus, red for a shortfall, grey where it balances exactly; large figures abbreviate (`2.4k`, `15M`) to fit under a 36px icon, and hovering names the item and gives the exact rate in words. There is no `+`/`-` on the number: the colour has already said which it is, and the characters are better spent on legibility.
- **Parts a group only makes for itself are left off that row.** The row answers what a folder *delivers*; Concrete produced and poured inside the same factory says nothing about that and crowds out what does. Hidden means precisely: produced in the group, consumed by the group's own production, nothing outside asking for it, and balancing. An intermediate that is short, or piling up with nowhere to go, is exactly what the row exists to surface and always shows. Shipping it anywhere counts as delivering it, including to a sibling in the same folder. **Options → Sidebar → Factory groups → Show group internal products** lists them anyway; it is off by default and saved in this browser rather than with the plan. The row itself can be turned off there too, under **Show group products**, which disables the internal-products toggle with it. The settings carry their explanations on an info icon rather than a paragraph each.
- **The planner draws the same tree over the cards.** A line in the group's colour runs down the left of the section, with an elbow into each card and a corner at the last, so a group reads as a block while scrolling rather than as a run of cards that happen to follow a heading. The band's own left edge is the top of that line rather than a border that happens to match it: full trunk width and squared off at the bottom, so it carries straight on into the cards. The elbow meets each card's title line; a card is hundreds of pixels tall, so the sidebar's midpoint would point at nothing. The rule that ends each card is dropped on the last one in a group, where it divides nothing and draws across the tree's corner. Ungrouped is deliberately left flat: indenting everything distinguishes nothing.
- **Grouped cards no longer wear the group's colour as their border.** With the tree saying where a card lives, the border was saying it twice, and it was saying it in the one channel that status needs, so a group's colour had to be taken back off any factory in trouble. The border now belongs to status alone; the group keeps the muted header tint, which does not compete with it.
- **The sidebar draws a group as a tree.** A line in the group's colour runs down the very edge of the section, starting at the header, with an elbow into each member factory and a corner at the last one. It is the shape a `tree` listing has, for the same reason: it says where a group starts and stops without a box around it. The rows themselves are unchanged, just indented off the line, and each elbow meets the middle of its row rather than its first line, so a factory carrying status chips is still pointed at squarely.
- **Collapsing a group is now instant.** It used to take seconds on a large group, because whether a group was open was stored on the group record, which is copied onto every member factory, so shutting a forty-factory group rewrote forty factory records, ran forty saves and forty metrics recalculations, and then tore down and rebuilt forty planner cards. Collapse is view state now: it lives on its own in local storage, touches nothing in the plan, and hides the cards rather than destroying them. A group that is already shut when a plan loads never renders its cards at all, so a plan kept mostly collapsed opens faster too. The trade-off is that collapse no longer travels with a shared plan or a cloud restore: it is per-browser, which is what it always should have been.
- **In the planner**, cards sit under a collapsible heading band per group, Ungrouped first. Bands only appear once there is more than one section, so a plan that has never used groups looks exactly as it did.
- **Colour.** Pick from a palette or any colour you like through the picker. Red and amber are deliberately not offered: they are the problem and warning tiers, and a group wearing one would read as a broken factory. A group's colour gives its cards a muted header and a bright left spine, **and a status still wins the border and the header**, so a factory short of copper stays red while its spine keeps saying which group it is in.
- **Every group ends with its own Add Factory button**, sat where the next factory in that group would go. One click makes a factory and puts it straight into that group, rather than adding one at the bottom of the plan and dragging it back up to where you wanted it. It is what the group's tree now ends on, the elbow reaching it from the side exactly as it reaches a factory row, and it gives an empty group — otherwise a header over nothing — a first factory without a drag. Ungrouped has one too, since "in no group" is a real destination and it is a long way down to the plan-wide **Add Factory** from there. A collapsed group hides it along with the rows it belongs to.
- **Assigning** is either a drag, or the new group chip on a factory's header, which lists the groups and can make a new one on the spot. It sits on the title line beside the factory icon, wearing the group's colour, deliberately not down among the status chips, because where a factory lives is not a thing that has gone wrong with it. A factory in no group says so: the chip reads *Ungrouped*.
- **Multi-group edit** does the same thing to a selection. Organising an existing plan one chip at a time meant opening forty cards in turn, so there is now a button beside **+ Group** that lists every factory grouped by where it currently sits, with *Select these* on each section for the case it is nearly always going to be: everything already in one group, or everything not in a group yet. Assigning does not close the dialog: the list re-sections in place and a toast says what moved, so several groups can be filled in one visit.
- **The multi-group dialog shows what each factory makes**, as item icons beside its name, and rolls the whole group up onto its header the way the sidebar does. Names alone are not enough to sort a plan by: "Copper Basics" and "Copper Ingots" are only distinguishable by what comes out of them. As many icons fit as the dialog is wide enough for, then a `+N` tile whose tooltip lists the rest, and every icon carries its item name on hover.
- **Groups can be renamed and reordered from that dialog too**, rather than only from the sidebar: a pencil beside each name (or a click on the name itself) opens it for editing, and up/down arrows move the group, disabled at each end of the order. Renaming applies on Enter and the field closes, so nothing is left looking half-edited. The sidebar's rename does the same now, where it previously needed a click elsewhere to commit and gave no sign it had.
- **Deleting a group never deletes a factory.** An empty group goes immediately; one that still holds factories asks where they should go first: Ungrouped, or another group.
- Groups travel with the plan through save, share links, templates and cloud sync, with no migration and nothing to set up. Plans made before this change load untouched, entirely ungrouped.
- **A group travels even before you put anything in it.** A group's record lives on its member factories, which is what carries it through every save, share and restore for free, but that leaves a group you have made and not yet filled with nothing to ride on. Those are kept on the plan itself, so they survive a copy and paste, the wizard's backup file, a share link, and a restore from your account. Replacing a plan takes them with it, too: pasting over a plan no longer leaves its empty folders behind in the new one. A plan of nothing but empty folders reaches your account as well, see below.

### Factory icons

- Every factory drew the same generic industry glyph, so a plan of twenty factories showed twenty identical icons and the only thing distinguishing a sidebar row was a truncated name. Factories now carry an **icon you choose**, and it shows everywhere the factory is referenced: the card header beside the name, the sidebar row, the Factories Summary table, the graph node, the import rows, and the import/export chips on collapsed cards and in the satisfaction table.
- **352 icons to pick from.** Real game art for every machine, extractor and generator, every belt, lift, splitter, pipe and pump, all the power infrastructure, the stations and storage, the vehicles, the equipment, and every raw resource, fluid and component the planner knows about, plus emoji: coloured squares, circles, diamonds and triangles, the digits 0–10, and a row of symbols. No uploads: a shared plan can only ever carry an icon the app already ships, so there is nothing to process on anyone's behalf.
- **The picker opens on everything.** The first view is the whole set, laid out under category headings, so you can scroll it like a sheet rather than hunting through tabs for where something lives. A row of category buttons (All, Buildings, Power, Logistics, Vehicles, Raw Resources, Fluids, Components, Equipment, Emoji) narrows it when you want that, and stays on screen the whole time so the categories double as a map of what is in there.
- **Search covers everything at once, and filters as you type.** A query searches the whole set regardless of which category is selected, so a narrowed view never hides a match; picking a category clears the query again. Emoji carry keywords, so "red" finds the red square, the red circle and the red triangles, and "nuclear" finds the radiation symbol. Names are matched with their punctuation stripped too, because the game writes "Mk.5" and nobody types the dot: "conveyor belt mk 5" and "mk5" both find it.
- Click the icon in the card header or the sidebar to open the picker; it takes the same hover fill as the clickable item and building images elsewhere in the app. A tile applies immediately and closes, and "Use default" puts the generic glyph back.
- **A factory stores only an icon ID.** Everything about how that ID is drawn, from the artwork it points at to its label, its grouping and even which emoji character it means, lives in one generated registry file. Plans in local storage, in the cloud and in share links can't be migrated, so nothing about rendering is baked into what gets saved: the artwork behind an icon can be changed later without touching a single plan.
- The registry is generated from the shipped artwork rather than from the game data, which means an item whose image was never shipped simply isn't offered. That caught the FICSMAS Gift, which is not flagged as a FICSMAS item and would have rendered as a broken image. A test asserts every icon in the registry resolves to a file that exists, so a bad entry fails the build instead of reaching the picker.
- Plans saved before this change are untouched and show the default glyph until an icon is picked.
- **Mael's MegaPlan wears them too**, all 36 factories, so its imports and exports read at a glance rather than as a wall of identical glyphs.
- **The Demo plan wears them.** Every factory in it carries the icon of the thing it makes, and it opens with two folders rather than none: the copper chain in its own copper-coloured group, and **Power** holding the whole nuclear chain from the Uranium Mine through to Plutonium Processing, alongside the Alien and Geothermal generators. A new plan shows both features, and shows that a plan can have more than one group. The two nuclear factories dropped the ☢️ from their names with it: the nuclear plant and fuel rod icons say the same thing better.

### A tab left open across a release can no longer overwrite your saved plan

- **The planner tells the API which version it is, and the API refuses saves from a version that is too old.** What this fixes: v0.6 saves your whole tab to your account (the power target, the planner version, groups with no members yet), where v0.5 saved only the factory list. A v0.5 tab left open in another window would autosave its list straight over the richer document, quietly destroying that plan-level state with no undo and nothing on screen to say it had happened. A client too old to save is now refused before it can do any damage. Closes [#166](https://github.com/satisfactory-factories/application/issues/166).
- **Refused means "reload", not "something broke".** The page blocks with an update notice naming both versions and a reload button, syncing stops so it isn't retried in a loop, and your work is left exactly where it is: it lives in this browser, and reloading does not lose it. Deliberately distinct from the sync-error path, which says the server is down and asks you to report it on Discord; a required reload is neither an outage nor something anyone needs to report.
- **Reads are never refused.** Loading your plan or opening a share link still works from any version, and carries a marker that tells an idle tab it has gone stale without it having to attempt a save first. `GET /hello` and `GET /health` report the current minimum unauthenticated.
- The comparison is *older than*, never *different from*, so a client newer than the API expects is allowed through and neither side can lock the other out during a rollout. The minimum comes from a `MIN_CLIENT_VERSION` environment variable read per request, so it can be raised with a restart rather than a rebuild. It defaults to the release that introduced the gate, which means the protection is on whether or not anyone sets it.
- The version itself now comes from one place (the repo root `package.json`, stamped into the build) rather than being a number nobody had been keeping up to date.
- The backend has its first tests: the version comparison is a pure module with a Vitest suite covering ordering, prereleases, missing and unparseable versions, and a misconfigured minimum. `pnpm test` in `backend/` runs them, and CI runs them on every PR.

### You get told when a new version of the planner is out

- **A tab left open across a release now says so, and offers to reload.** A small message appears at the bottom of the page naming the version that is live. Dismiss it and carry on, or reload when you are at a sensible point. Nothing is blocked and nothing is lost either way: your plan is stored in this browser and reloading keeps it. Closes [#166](https://github.com/satisfactory-factories/application/issues/166).
- This is separate from the update notice that blocks the page. That one only appears when the API refuses to save from a build too old to be trusted with your plan. This one is for every other release, where the build you are on still works perfectly well and there is simply a newer one.
- **You do not need an account.** The check is a public `GET /version` on the API, so anyone with the planner open gets told, signed in or not.
- The check runs once a minute, pauses while the tab is in the background, and asks again the moment you come back to it. It stops once it has told you, and a version it has already mentioned is never mentioned twice in the same tab. If the API cannot be reached, nothing is shown at all.

### Cloud sync: an emptied plan is still a plan

- **Deleting your last factory now reaches your account.** Syncing refused to save a plan with no factories in it, so an emptied plan left the previous version sitting in the cloud and the next restore handed back the work you had deleted. The same rule blocked a plan holding nothing but groups you had made and not yet filled, which is real plan state now that groups travel on their own.
- The reason that rule existed is real, so it has been replaced rather than removed: an empty plan is also what this browser looks like in the seconds before it has loaded your account's copy, and the two are indistinguishable. An empty plan is now allowed to be saved only once this session has seen what the account holds: loaded it, confirmed the local plan was ahead of it, found the account empty, or written to it already. A sync landing in the gap between logging in and the load finishing still cannot upload nothing over everything.
- **Restoring from your account brings the plan's name back with it.** The whole tab is uploaded, name included, but the restore was putting back the power target, the planner version and the groups and then leaving the name as whatever the local tab was called, so a plan saved as "Nuclear rework" came back as "Default". The tab's own ID stays local, since that is what this browser keys its tabs by.

### Backend: a health check that can actually fail

- The API had one health route, `GET /hello`, and it returned `200` for as long as the Node process was alive. It never spoke to Mongo, so the database could be dead, unreachable or out of disk and the monitoring would still show green, which is exactly what happened when the API box filled its disk. New `GET /health` runs a real `ping` against Mongo (the `SELECT 1` equivalent) and returns **503** with the error message when it doesn't answer, so an outage raises an alert instead of nothing. The response also carries process uptime, the Mongoose connection state and how long the ping took.
- The ping is raced against a 3 second timeout. Mongoose is configured with `bufferCommands`, so with the database down a command doesn't fail. It queues for ten seconds, which is longer than any monitor or the Docker healthcheck will wait, and a dead database would have looked like a slow one.
- `/health` gets its own rate limit of 10 requests a minute and is exempt from the global limiter, so no amount of other traffic can push a monitor into a `429` and a false outage.
- The production container's Docker healthcheck now probes `/health` rather than `/hello`, so `docker ps` reports unhealthy when Mongo dies rather than only when the process does. Consequence: a deploy attempted while Mongo is down now fails at `up -d --wait` instead of going green. **The box's compose file is a hand-maintained mirror and no deploy syncs it**, see `docs/deployment.md`.
- `/hello` stays as-is, as a pure liveness route.

### Building groups: Satisfy and Trim, one group at a time

- **Every group row carries its own Satisfy and Trim**, putting the item's whole gap on that one group and leaving the rest alone. Before this, the only ways to close a gap were to spread it across every group (**Evenly balance**) or to push it onto the last one, neither of which is what you want when one group is a set of miners on a node you have already committed to. The group keeps its building count and the clock is rescaled to hit the new output; the count is only re-solved when the clock would leave the game's 1-250% range. Where no setting of the group could absorb the change, the button is shown disabled with a tooltip saying so, rather than being hidden.
- They are deliberately the same green up-arrow and yellow down-arrow as the product's own Satisfy and Trim, which do the same job one level up: the product pair matches the *product* to what the factory and its exports ask for, the group pair matches the *groups* to the product.
- **Balanced is now a percentage of what the item asks for**, defaulting to 1% and capped at ten effective buildings' worth, set under **Options → Building groups → Effective output tolerance**. It was a flat 0.1 effective buildings, carried as a separate copy in the status line and in the engine's problem flag. One effective building is 60/min on a mine and 15/min of Iron Rods, so the same allowance meant completely different things per recipe, and a 360/min mine read as balanced while 6/min short. The cap is what stops the percentage loosening anything as an item grows: 1% of a 100-building factory would otherwise be a whole building of drift. Zero is not offered, because some recipes cannot be balanced exactly.
- The tolerance is stored per browser rather than with the plan, and is range-checked on restore: a stored zero or negative would paint every plan red with nothing on screen to explain it. Changing it forces a recalculation, since the problem flag is written by the engine and saved into the plan, so the tray, the sidebar and the factory status would otherwise keep the old verdict until some unrelated edit.
- The Options dialog demonstrates the tolerance with a live building group rather than a paragraph. It is a real factory driven by the same engine calls the planner's own group row makes, so it cannot show behaviour the planner does not have.

### Satisfy and Trim say what they will set

- **Every Satisfy and Trim button now names the figure it would land on**, in brackets on the button itself: **Trim (480)**, **Satisfy (240)**. They used to be a leap of faith — you pressed one and read the result afterwards, and undoing it meant remembering what the number had been. All three levels carry it, and each names the number in its own units: a product's new Qty/min, an import's new quantity, and for a building group the output that group would be left producing (MW for a power generator's group). A button with no answer to give — a half-filled import row, or a building group no setting could balance, which is disabled anyway — simply reads as it always did rather than claiming a target of "(0)".

### Imports: trim a request to what the supplier can spare

- **An import that asks for more than its supplier makes now offers to trim itself to fit.** Nothing stopped a factory requesting 1,015 Dark Matter Residue from a plant that only produces 900: the request went in, and the 115 it could never deliver surfaced on the *supplier* as an unmet export request, a long way from the row that caused it. The import row now carries a **Trim to Capacity** button whenever it over-asks, naming the figure like every other Trim does.
- **Capacity is what the supplier has left**, after what its own production lines and power generators consume, and after every promise it has already made to other factories. So a plant making 400 Diamonds and eating 96 of them itself can spare 304, and where two factories draw on the same item the row being trimmed takes what is left rather than what the other one already has. Its own other rows against the same supplier count too, since those are separate promises the supplier still has to keep.
- **It only ever shrinks a request.** A row that already fits is left exactly as you set it, so the button is not a second Satisfy: Satisfy sizes an import against what the importing factory *needs*, and this sizes it against what the supplier can actually *give*. The two answer different questions and can both be on offer at once.
- Trimming doesn't make the shortfall disappear, it moves it to where it belongs: the supplier goes green, and the importing factory shows the gap it has always really had, with the usual Satisfy and shortage controls to go and solve it.
- The button hides itself when there is no capacity to trim to at all — a supplier with nothing spare leaves the row nothing to snap to, and a quantity of zero is not a valid import.

### Imports: satisfy a request to what the supplier can spare

- **A shortfall bigger than its supplier can cover now takes one press, not two.** A factory 5,000 Limestone short of a supplier with 2,600 spare had only **Satisfy**, which sized the row against the 5,000 it needed, immediately over-asked the supplier, and put a **Trim to Capacity** button on screen to take it straight back down to the 2,600 you could actually have had. **Satisfy to Capacity** lands on the 2,600 in one go.
- **Every button on an import row now says which of the two questions it answers**: **Satisfy to Need** and **Trim to Need** size the row against what this factory wants, **Satisfy to Capacity** and **Trim to Capacity** size it against what the supplier can actually give. All four name the figure they would set, as before.
- **Satisfy to Capacity only appears when it has something of its own to say**: it is hidden when the supplier can cover the whole need (Satisfy already lands within capacity), when the row already asks for more than the capacity (that is Trim to Capacity's job, and it is on screen saying the same figure), and when the supplier has nothing spare at all.
- Taking the capacity doesn't pretend the shortfall is solved. The supplier goes fully committed and green, and the importing factory keeps the gap it always really had, with **Satisfy to Need** still on offer for when you find it a second supplier.
- The buttons now wrap onto a second line rather than overflowing the row, so a row carrying both a Need button and a Capacity button no longer pushes **View** and the delete button off the edge of the card.

### Factory status indicators

- A factory in the sidebar used to tell you exactly one thing: it was red, or it wasn't. `hasProblem` was a single boolean, so three unrelated failures collapsed into one red blob and anything short of outright broken had nowhere to appear. Factories now carry a **status list**: named, severity-tiered conditions rendered as chips under the factory name, so a 30-factory plan can be triaged without opening a single card.
- **Three severity tiers.** Red for a problem: the factory can't do what the plan says it does, because some quantity doesn't add up. Amber for a warning: the plan is coherent, but something is probably not what you meant, or your world is behind your plan. The highest of those two paints the border; chips are not collapsed, so a factory that is both short of copper *and* out of sync shows both. The third tier is a note, worth counting but very often deliberate: it gets its chip, outlined rather than filled, and the factory stays green.
- **Nine statuses to start with.** Red: shortage, unmet export request, and building groups that don't add up. Amber: unhandled byproduct, out of sync with the game, redundant import, and duplicate import. Game sync moved into the amber tier rather than being a state of its own, and it shares the burnt orange it always wore, now a proper `--sf-status-warning` token instead of borrowing the building colour. The note tier holds two: **no demand** and **potential blockage**.
- **No demand counts the products nothing asks for.** A product no recipe in the factory needs and no other factory imports. Zero demand only: a partial surplus is a surplus, and the product row already offers to trim it. It stays out of the border colour on purpose, because a factory whose job is to feed the sink or a Dimensional Depot is doing exactly what you built it for. The chip appears beside the product, beside the item's surplus in Satisfaction, and as a count on the Factories Summary.
- **A byproduct nothing consumes will back up and stop the buildings making it.** You can decline to build a product nobody wants; you cannot decline the Heavy Oil Residue or the Plutonium Waste that comes off the line. How loudly the planner says so depends on whether the AWESOME Sink would take it, because that is the difference between a loose end and a wall:
  - **Potential blockage** for anything sinkable. There is a way out, you just haven't drawn it yet, so this is a note and the factory stays green.
  - **Unhandled byproduct** for fluids and for radioactive items, neither of which the sink accepts. Amber, and it does colour the factory. The product or generator responsible takes an amber row, as does the item's row in Satisfaction.
  Recipe byproducts and generator waste both count, and exporting it or blending it into a recipe that consumes it clears either status. The sinkable rule is the one the sink feature itself will use: no fluids (the sink has no pipe input), and none of the Uranium Waste chain, though the fuel rods themselves are fine.
- **End products say so, in blue.** Items the game itself never consumes are the end of their chain: the AI Expansion Server, the Ballistic Warp Drive, nobelisks, rebar, the Portable Miner. Having no consumer is what those are for, so they read **End product** rather than "no demand". The set comes from the recipes, counting every way the game takes an item back, so the nuclear fuel rods and the Alien Power Matrix are correctly excluded: nothing crafts with them, but generators burn them.
- **The chips say which items.** Every affected item shows its own icon alongside the count, so three shortages are three icons and "3 shortages" rather than a generic warning glyph. Up to four icons show in the sidebar, with the full list in the chip's tooltip. The section headers inside the card show the same status in fuller form, up to six icons plus a longer label, so the sidebar can stay terse and the card can be specific. Clicking a chip scrolls to the section it points at.
- A factory with nothing wrong renders no chip row at all, rather than an empty one that still took its share of the spacing in the card header and left a gap before the first chip that *was* there.
- **The sidebar's minimum drag width went from 150px to 300px.** A chip carrying several icons and a label ("3 shortages") is a single unbreakable box, and the sidebar hides its horizontal overflow, so below about 200px the label was silently cut off mid-word, which is the one thing the chip exists to say. A width saved below the new floor is raised to it on load rather than waiting for the next drag.
- **The sidebar entry grows and retracts** rather than snapping: the colour lands first, then the row expands to make room for the chip, and it collapses again when the condition clears. Loading a plan renders any existing chips already open, so a plan with a dozen broken factories doesn't play a dozen animations at once.
- The same treatment now applies everywhere factory state was already painted: the sidebar entry, the factory card's border and header, the section headers (Satisfaction, Imports, Products & Power), and the Factories Summary, which never painted the amber state at all, and whose factory names now carry a tooltip listing their statuses.
- **The Factories Summary counts what is wrong, and the counts are filters.** Its header, and its row in the sidebar, show how many factories are short of parts, how many have building groups that don't add up, how many are out of sync, and so on for every status. One chip per kind rather than a rollup that double-counted the same factory. Clicking a count in the header lists only the factories behind it, so "6 shortages" becomes six rows you can jump into; clicking it again clears. The header carries them whether the summary is open or collapsed, and a healthy plan shows nothing at all.
- The summary's import and export chips name the other factory with **its own icon** rather than a generic arrow or truck, matching every other place a factory is referenced.
- **Fixed:** a factory whose *power generator* building groups didn't add up never turned red. The check has always run for power producers, but the rollup only ever looked at products, so a power-only factory could be visibly broken and still read as healthy.

### Planner: use the whole window

- **A "Full width" button** in the sidebar's global actions, beside Hide all and Expand all, drops the margins the planner keeps on a wide screen and gives the plan the entire window. Those margins exist so a factory card doesn't stretch into an unreadable line on a big monitor, but on a 2560px screen they were costing roughly 500px — a quarter of the width — that a plan with wide satisfaction and summary tables would rather have. Which of the two you get is now your call: the button reads "Full width" while they're on and "Normal width" while they're off, and it fills in solid to show which state you're in. The setting is remembered per browser, like the other planner options, and below 2000px there are no margins to drop so it changes nothing.

### Sidebar: active factory indicator

- The sidebar's factory list now shows which factory you're currently looking at: an orange bar (the same orange as the selected tab's underline) on the left edge of that factory's entry, following you as you scroll the plan or jump between factories. No more losing your place in a 30-factory plan. A factory counts as "in view" when its card spans an eye-line 10% down the planner pane (not the pane's very top edge), so a factory scrolled slightly past its header is still credited as the one being viewed rather than the tail end of the factory above it. The Statistics and Factories Summary jump-links get the same treatment when their sections are in view, and while the eye-line crosses the divider gap between two cards the indicator stays put on the last entry instead of blinking off. Works in both the desktop sidebar and the mobile navigation drawer.
- **Every button in the sidebar now explains itself**, through the same tooltip the rest of the app uses. Some carried a native `title`, most carried nothing, and a native tooltip could never help where it was needed most: a browser shows none at all for a disabled control, so the five buttons that grey themselves out on an empty plan said nothing about why. Each now gives its own reason ("Nothing to copy yet — add a factory first") instead of just going grey. The group drag handle's hint worked nowhere at all before this: Font Awesome replaces its `<i>` with an `<svg>`, and an SVG element ignores the `title` attribute entirely.
- The thick divider between factory cards in the main view swapped its blue for the app header's burnt orange (now a shared `--sf-header` token, so the header, its gold border and the divider draw from one definition in `colors.ts`).
- In the Factories Summary sidebar row, the factory count chip and the fullscreen-summary expand control swapped places: the expand control now sits flush at the row's right edge, sized to the same 30px column as the factory rows' game-sync cells so the two line up down the sidebar.

### Export Calculator: belts

- The Export Calculator gained a Belts transport method (returning from the pre-rework calculator, now per-destination): a full-width section beneath Train/Drone/Truck/Tractor showing how many conveyor belts of a chosen mark it takes to carry the export. All six marks (Mk.1–Mk.6 at 60/120/270/480/780/1,200 items per min) are selectable via an icon toggle, and the default mark auto-picks the smallest single belt that can carry the amount. Belt speeds are maintained in code, because they aren't part of the game docs data. Fluid exports point to pipelines instead.
- Belt groups: an export can be split across multiple groups of belts, each with its own mark. Group columns fill left to right (up to four per row, divided like the transport columns, each group's controls centred within its column). Each group offers two linked inputs: an items/min amount (with the shipped item's icon, ingredient-chip style) **OR** a whole number of belts to build (with the belt icon): entering an amount derives the belts to build (rounded up), entering a belt count sets the amount to those belts' full capacity, so players can plan by exact throughput or by "I'll just run N belts". Over-provisioning is fine; only undercapacity (groups carrying less than the export) raises the red warning chip; otherwise a green "All good" tick shows beside the actions. Between the two sits an amber nudge for structurally redundant groups: if a whole group could be deleted with the remaining groups' built belts still covering the export (judged on whole-belt capacity, e.g. 8+1+1+1 belts for a 10-belt export, or one group whose belts already carry everything), the chip says how many groups are redundant. Deliberate headroom where every group is still needed is never flagged. Adding a group never rebalances fine-tuned amounts: the new group just picks up whatever the export still lacks (0 if fully allocated), with "Split evenly" available to rebalance on demand; per-mark total chips sum the whole belts to build across groups (three groups of 3× Mk.5 totals 9, not the ceiling of the fractional sum). A lone group tracks the export amount (never shrinking a deliberate over-provision on reopen). Groups persist with the plan (saved per part + destination factory on the export calculator settings, so they ride along in save/load/share; older saves initialize lazily).
- **Pipes for fluid exports**: fluid exports swap the Belts section for a Pipes section with the same group mechanics: Mk.1 (300 m³/min) and Mk.2 (600 m³/min) pipelines, pipe groups with even splitting, the All good / mismatch status and per-mark totals. Pipe groups persist separately from belt groups on the same export calculator settings. Pipeline Mk.1/Mk.2 icons pulled from the wiki into the building assets (64/256px).
- Fluid Trucks (added to the game in patch 1.2, 3,200 m³) are now supported: the Truck column calculates fluid exports as "Fluid Trucks" with the proper Fluid Truck (tanker) icon, a new vehicle asset pulled from the wiki, instead of telling you to package the fluid. Drones and Tractors genuinely cannot carry raw fluids (drones/tractors have no fluid variant; packaged fluids are solids and travel as normal cargo), so for fluid exports those two columns are greyed out with a note saying to package first. The Fluid Freight Car stays at 1,600 m³.
- Tidied the Export Calculator tray: it now reads as an actual recessed tray (darker background with an inset lip, breathing room top and bottom), and the mixed thick/thin borders between the transport columns were unified into single thin dividers matching the satisfaction table's row lines. The dividing lines under the accuracy chips are gone, so each column's header visually attaches to its content. The tray also grows with its content instead of clipping at a fixed 250px height (scrolling beyond ~800px).
- Calculator polish: the Train's round-trip input icon is now a train instead of a refresh glyph; the Drone column gained a disabled "N/A" placeholder where the other vehicles have Start Timer (its rows now line up, helped by no longer reserving validation-message space under the drone time input); the belt group delete button matches the factory header's square outlined red trash button; and `tooltip-info` icons no longer sit ~3px below the surrounding text's baseline (FontAwesome's -0.125em shim, applied app-wide).

### Satisfaction: jump to a requesting factory

- Each export chip in the satisfaction table now carries a small blue eye button on its right-hand edge that jumps straight to **the import row taking that export**, rather than to the destination factory's card. Clicking the chip itself still selects that destination in the Export Calculator, which was the only thing a chip click could do before. Reading a factory's name and having the calculator open instead of the factory was the wrong assumption often enough to be worth its own control. Landing on the card was the right factory but still the wrong place: its Imports section is a list, and on a factory importing a dozen things you were left hunting for the row that named this export. The part chips under a collapsed factory's "Exporting:" list do the same, since they know the part as well as the destination; the factory chip they sit in still jumps to the factory itself. Where the row can't be found (a half-configured import with no factory or item chosen yet) the jump falls back to the destination's Imports section.
- **An import's View button makes the same trip in reverse**, landing on the product that supplies the import rather than on the supplying factory's card. A part supplied as a byproduct has no row of its own, so the jump lands on the product that makes it; a factory exporting a surplus of something it imports produces it nowhere, and falls back to its Products section. Behind both directions, a jump that finds neither its row nor its section — a card that has yet to render — now falls back to the factory card itself rather than doing nothing at all.
- **Every jump now says where it landed.** Whatever the planner scrolls you to — an import row, a factory card, a section, a satisfaction row from a status chip — pulses white for about a second on arrival, so the eye finds the thing that was jumped to instead of searching the screen for what changed. Cards and sections pulse their header rather than their whole body, which would wash half the screen. The pulse starts as the scroll lands rather than as it sets off, and it holds most of its brightness through the middle of the animation, so it is still going when you get there. Respects `prefers-reduced-motion`.
- **A status chip lights every row it is about.** A chip reading "3 shortages" is about three rows, so the jump lands on the topmost of them and pulses all three rather than picking one and leaving the other two to be found by eye — the same for unmet exports, redundant imports and anything else a chip counts. Rows that have yet to render are picked up by the same correction passes that fix the scroll, so a card still materializing does not cost them their flash.
- **The unhandled byproduct chip flashes what it lands on.** A byproduct has no row of its own, so the product row that makes it carries an empty, zero-height marker with the byproduct's id purely so the jump has somewhere to go. Flashing a box with no height lit nothing at all, and on the very same product row the No demand chip pulsed correctly, which made it look like the byproduct chip's jump was broken rather than invisible. The pulse now goes to whatever owns the marker, so both chips light the same row.
- **An "Exported" chip** now sits beside Product and Imported on any item another factory has asked for, so what leaves a factory reads off the item's own row rather than only off the Exports column. White, to match the factory chips that column shows.

### Tasks: reorder by dragging

- **Tasks can be dragged into any order**, by the grip handle on the left of each row, the same handle the sidebar's factory list uses. A task's position was fixed at the moment it was added, so the only way to reprioritise a list was to delete a task and retype it at the bottom. Completed tasks drag too.
- **Done is now a checkbox** rather than a pair of buttons, and it has moved to the left of the task, next to the handle: an empty box while the task is open, filled green with a tick once it is done, matching the green struck-through title beside it. The old control was a blue button that turned into a grey one, which read as an action to take rather than a state to see at a glance.
- **A typed task is added when you click away from the field**, not only when you press enter. Typing a task and then going back to the plan used to throw the text away silently, which is the one thing a notes field must never do. Enter still adds it, blank and whitespace-only entries are still ignored, and the title is trimmed.
- Delete stays on the right and is now outlined rather than a solid red block, since it was the loudest thing in the card while being the action you least often want.

### Improved: Statistics say where a number came from

- **Raw Resources counts what the plan digs up, and is a table.** It used to read the raw supply the planner was assuming on your behalf, so with the assumption gone it had nothing to read and sat empty on exactly the plans that mine properly, the Demo plan included. It now sums every product whose part is a raw resource, whatever produced it: Copper Ore 360/min, Crude Oil 960/min, Limestone 180/min, Sulfur 240/min, Uranium 240/min and Water 2,640/min on the Demo plan. Consuming a resource is not extracting it, so a factory importing ore does not count that ore a second time.
- **Product Surplus & Deficit and Produced Items are now one section, Item Production.** They listed the same items twice: one with the balance, one with the supply. It lists every item, with a search box and filters for surplus, deficit and balanced. The filter counts follow the search. The "Show all Products" toggle is gone with the section it controlled.
- **Item Production and Raw Resources are tables**, and say where each item comes from. Every factory producing it is listed with what it makes, in green, alongside any factory short of it, in red. Click a chip to jump there.
- **Building Summary is a table too**, listing every factory that holds each building type and how many stand in it. A building requirement that has dropped to zero is no longer counted.
- **Power Consumption and Generation gains a per-factory breakdown**, collapsed by default so the plan-level figures it sits under are still the first thing you read. Generated, consumed and the difference for each factory, heaviest net drain first (the factory worth looking at is the one costing the most, not whichever the plan happens to list first), and a row click jumps to it. Deliberately simpler than the tables above it: no minimum and maximum columns, because the question here is which factory is costing what.
- **Each group in the sidebar can say whether it pays for itself.** The same generated / consumed / balance chips the Statistics link above them wears, minus the target: a target belongs to the plan, and a group cannot have one of its own. Off by default, since power is a question people ask of the plan more often than of a folder within it; turn it on under **Options → Sidebar → Factory groups → Show group power**.
- **A group says what is wrong inside it**, in the sidebar and on its band, with the same chips the Factories Summary uses for the whole plan: how many of its factories have problems, how many are short of parts, how many are out of sync. Under the group's name and above what it makes, and absent entirely when nothing is wrong.
- **The circuit boost has its own chip** on the factory header, the group band and the sidebar group, wherever Alien Power Augmenters are involved. An augmenter adds a percentage of the *whole plan's* generation, credited to whichever factory holds it, so a group of two augmenters generating 3.3 GW between them read as 61 GW with nothing on screen saying why. The chip names the figure and its tooltip says it is counted in the plan's power rather than that group's.
- **The band above a group's cards** carries the same colour swatch the sidebar header does, and opens the same palette. Its chips are ordered by what they answer: the name, factory count and problem count on the left, what the group costs on the right.
- Two collapsed groups sat flush against each other with no gap between them, and in the sidebar a collapsed group's bottom corners stayed square while the group's colour line hung a few pixels past the header into the gap below.

### Fixes

- **A group's factory count no longer clips its own number.** The sidebar group header lays its parts out in a row, and when the sidebar got narrow the count chip was the one that gave way, so a two-digit total was cut off against the chip's border. The chip now holds its width, as do the expand and delete buttons beside it, leaving the group's name as the only thing that gives ground. Its padding is even again too: the number used to sit with 8px to its left and 24px to its right, which read as wedged against the icon.
- **The X on Item Production's search box now clears it, and stays put while there is something to clear.** Font Awesome is loaded here as the SVG-replacing script, which swaps out each `<i class="fas fa-*">` for an `<svg>` of its own — including the one Vuetify renders for a field's clear button, and along with it the very DOM node Vue had attached the click handler to. The X drew perfectly and did nothing, in every `clearable` field in the app: Item Production, Parts & Recipes and the factory icon picker. Vuetify is now handed a component for that icon rather than a class name, which nests the glyph inside a wrapper Vue owns, so Font Awesome replaces only the inner element and the handler survives on the outer one (which is also what carries the button role and its label, so the fix reaches assistive technology as well as the mouse). Separately, the X used to fade in only while the box was focused or hovered — type a query, click a filter button, and the way back out of the query had vanished — so on the Item Production box it is now pinned for as long as there is text in it.
- **"Correct Manually" no longer blocks the fix for a byproduct the game can make on purpose.** A shortage of an item this factory produces as a byproduct was always treated as a dead end: the planner said it could not scale byproducts for you and left it there. That is true of Dissolved Silica, which falls out of Quartz Purification and which nothing in the game makes deliberately, and of nothing else. Dark Matter Residue comes off every Quantum Encoder recipe *and* is made outright by a Converter, so a Phase 5 factory burning more of it than its encoders drop was told to sort it out by hand, while the buttons beside it happily offered to build it in a new or existing factory — every factory except the one that needed it. Being a byproduct here no longer counts against making it here: such an item gets the ordinary **+ Product** button, adding the Converter recipe at exactly the shortfall, and only a genuinely byproduct-only item keeps **Correct Manually**. The two buttons are now exclusive, so the row says either "you can fix this" or "only you can fix this", never both.
- **A part with no recipe of its own no longer offers to have another factory make it.** "Add to factory" was shown for any shortage, but building a product needs a recipe, and Dissolved Silica and the three power slugs have none — the new factory came out with an empty product row and the import pointed at it supplied nothing. Those parts now show only the button that tells you the truth.
- **"+ Product" picks a recipe that actually makes the part.** The default recipe was chosen from every recipe the part appears in, byproducts included, so Heavy Oil Residue defaulted to *Plastic* and Compacted Coal to *Ionized Fuel*. A product's amount is read against its recipe's primary output, so the row read "300 Heavy Oil Residue" while sizing the buildings for 300 Plastic. Only recipes that make the part outright are considered now (Alt: Heavy Oil Residue and Alt: Enriched Coal respectively), and unpackaging is picked last, since the packaged form has to come from the unpackaged one first.
- **Tooltips and toasts now escape the text they are given.** Both build their contents from plan data (part ids, factory names, group names) and both rendered the result as raw HTML. A share link decides all three, and an item id the planner does not recognise is echoed back verbatim, so a crafted plan could put markup into a tooltip and have the browser run it on the page of whoever opened the link. The strings do genuinely need a little markup, since the tooltips use line breaks and bold throughout, so the text is now escaped and a fixed list of formatting tags is restored afterwards. Attributes are never restored, which is what makes a payload inert even on a tag that is on the list. The hover tooltip used for game assets already refused to render HTML for this reason and carries a comment saying so; the other three places did not get the same treatment at the time.
- **A chip's shape now says whether you can press it.** Everything in a factory card header was squared off to match the buttons at its right edge, so the power, power shard and Somersloop readouts (which do nothing at all) looked exactly like the tasks, notes and sync chips, which do. Pressable chips keep the square corner; anything that only reports a number is a pill, everywhere in the app. Status chips on a section heading were the reverse case: they were drawn as buttons, and given a tab stop and a pointer cursor, but nothing was listening. Clicking one did nothing, because you are already in the section it would have jumped to. Those are now plain labels, while the same chips in the sidebar and on the card header still jump.
- Status chips in a factory card header sat two pixels below the chips beside them. The chips grow onto their own line in the sidebar and carry a gutter to keep them off the factory name above; on a shared line that gutter pushed them down instead. It now applies only where they actually stack.
- The Factories Summary no longer sits flush against the first group band beneath it, and its factory count is a pill rather than a squared-off chip that read as a button.
- **No game image anywhere in the app had a tooltip.** Hovering an item or building icon that wasn't a wiki link told you nothing, so a row of eight icons was eight guesses. The name was being passed down, but Vuetify's image component neither renders it nor forwards it to a root element, and the component wrapping it had three root nodes, which is a shape Vue drops stray attributes from silently. Every game image now carries its display name, including the group product line in the sidebar.
- **Nothing said a factory's name or a task was editable.** Both are plain text until you happen to click them, and the factory name went further and showed a pointing hand on hover, the cursor for a button, on a field you type in. Both now show a text caret and underline the text under the pointer, and stay underlined while you're editing. The underline marks the words rather than filling the field, because the name input runs to most of the width of the card header and a highlight that size drew far more attention than the edit it was offering.
- **Deleting an import, or deleting a factory that supplies another one, silently stopped half way.** The plan wasn't recalculated afterwards, so the supplying factory kept producing for an import that no longer existed; deleting a factory also skipped the re-ordering, leaving a gap in the sequence (removing the first of nine factories left the rest numbered 1–9 instead of 0–8). The calculation engine copies the plan with `structuredClone`, which refuses to copy a Vue reactive proxy, and both of those actions stored exactly such a proxy: taking `factory.inputs.filter(...)` off a reactive factory reads its rows out *through* the proxy, so assigning the filtered array back puts the proxies in as the new rows. Everything after the copy was then skipped by the error. Deleting a factory only tripped it when a dependant still had another import left over: filter an array down to nothing and there are no rows left to be proxies, which is why it went unnoticed. Every place that rewrites an array this way now unwraps it first, and the engine falls back to a slower deep copy (loudly, in the console) rather than abandoning the calculation if one is ever missed again.
- A building group no longer appears to accept more Somersloops than the building has slots for. Typing 5 into a Quantum Encoder (or a Blender, or any other 4-slot building) left the 5 sitting in the field, which reads as though the planner took it. The calculation was always correct, because the engine clamps to the slot count before it does anything with the value, but the number on screen disagreed with the number being used. Entries are now clamped as they're typed rather than on the debounce that follows, and the field is re-rendered with the clamped value, so what you see is what the plan is built from. Slot counts themselves were already right for all ten amplifiable buildings (Smelter and Constructor 1, Assembler, Foundry, Refinery and Converter 2, Manufacturer, Blender, Particle Accelerator and Quantum Encoder 4) and are now asserted per building, along with the clamp, so a wrong cap can't slip in unnoticed.
- The Somersloop field's up arrow now greys out once the building's slots are full, so the cap is visible before you hit it rather than only being explained by a warning afterwards. Vuetify disables that arrow off its `max` prop, which can't be used here: with `max` set the field stops reporting out-of-range entries at all, which is exactly what the typed-value clamp above needs to see. So the increment button is supplied through the component's own slot and disabled against the slot count instead.
- **The Factories Summary states a power-only factory's output too.** Its Products column was built from products alone, the same blind spot the collapsed card had, so a factory of nothing but generators left an empty cell in the table meant to summarise the plan — reading as a factory that does nothing. Generators now get a chip there beside the products, green and bolt-led like everywhere else power is stated, showing what each puts on the grid with the generator's name and building count on hover. The column is headed "Products & Power" rather than "Products", since that is now what it holds.
- **A collapsed factory that only generates power no longer calls itself empty.** The collapsed card built its Producing row from products alone, so a factory of nothing but generators — a coal farm, a fuel island — reported "Empty factory! Select a product!" over a plan happily counting its 1.35 GW in the header two lines above. Generators now get a chip in that row alongside the products, green and led by the same bolt-plus the generator's own power chip wears when the card is expanded, then the building's icon, the generator's name, how many of it there are and what it puts on the grid (`Coal-Powered Generator: 8x (+600 MW)`). The bolt earns its place: a building icon on its own reads as a product, as though the factory were manufacturing generators rather than running them. Only a factory with neither products nor generators is called empty now.
- Editing a building group's Somersloops no longer spins the refresh icon on the building count as well. Every input in a group shared one debounce key, so whichever field you touched, both spinners lit up; each input now has its own key and only the field you're editing spins, the same per-field behaviour the product and power generator rows already had.
- Building group chips line up again when one of them carries a wide caption. A group's chips each sit above their own caption, and a Converter running Iron Ore (Limestone) at 51% puts "461.9 MW (184.8 MW – 739.1 MW)" under the clock, far wider than the input above it, which was left hanging 14px off centre. Chips and captions are now centred on each other, so the row reads as one line of inputs no matter what's written beneath them.
- Refreshing or deep-linking the Parts browser no longer 404s. The site is a single-page app, and only `index.html` exists on the server, so every route needs a rewrite in `web/vercel.json` pointing it back at the entrypoint. That was a hand-maintained list of routes, and `/parts` was never added to it when the page shipped; navigating there from inside the app worked, so only reloads and shared links broke (and the dev server serves every path from `index.html` anyway, which is why it never showed up locally). Rather than add the missing entry, the list is gone: a single catch-all now covers every route, so adding a page is once again a zero-config operation. The catch-all deliberately excludes `/_vercel/*`, which Vercel serves itself for Web Analytics and Speed Insights. Swallowing those would feed the browser HTML where it expects JavaScript and kill both, silently.
- Unknown URLs now land on a proper "Page not found" page with links back to the planner and the parts browser, instead of the bare CDN 404. This is a consequence of the catch-all above: the app, not the CDN, is now what answers for a URL that matches nothing.
- The hidden sidebar's hover tray no longer sticks open when the cursor leaves the window. Flinging the cursor onto a monitor to the left (or alt-tabbing to the game) used to trip the left-edge peek zone on the way out and leave the tray showing indefinitely. A peek triggered by the cursor exiting the window is now provisional (it closes after a second unless the cursor comes back in), and the tray always collapses when the window loses focus. Peeking by hovering the left edge inside the window behaves exactly as before.
- Reordering a group (or dragging a factory between groups) from that same hover tray no longer collapses it mid-drag. Starting a drag fires a mouse-leave on the row underneath, and no mousemove arrives while a drag is in flight to peek the tray back out, so the sidebar slid shut the instant you picked a group up, taking every drop target with it. The tray is now held open for the duration of any drag started inside it, and hands itself back to the usual peek rules once the group is dropped — a sidebar hidden before the drag is hidden again as soon as the cursor leaves it.
- **Every automatic correction the planner makes to your plan is now reported in one place.** The dialog that told you about micro-rounding repairs now covers everything the loader puts right, grouped by factory: broken import/export links, two factories that ended up sharing an internal ID, the same item imported twice from the same factory, an import of something that isn't made any more. Each line says what was wrong and what was done about it, in plain terms. The browser alert that used to fire for these ("check the browser console for more details") is gone. It appeared before the planner had drawn anything, said nothing useful, and sent you to the developer console. That detail is still logged for anyone debugging a shared plan.
- The debug template behind "#485: Micro-rounding repair" is now "#485 + #499: Rounding & broken chain repair", and carries a broken import/export chain alongside the drifted quantities: a copied factory inheriting the original's exports, an export that no longer matches its import, an import the supplier has no record of, an export entry pointing nowhere, and two factories sharing an internal ID. Loading it raises one dialog listing both kinds of correction, with the Refinery and its copy each showing both under a single heading.
- **Loading a plan now checks the whole import/export chain and repairs what it finds.** A plan whose figures look current is deliberately not recalculated on load, which is what makes switching tabs fast, so anything already wrong stayed wrong through every reload. The check runs on every load, fixes what it can, recalculates, and tells you what it changed.
- A plan that had never been calculated, a template or one built in code, could have its perfectly good imports and exports torn up on its very first calculation. Two checks concluded "this factory doesn't make that" from an empty satisfaction ledger, which at that moment simply hadn't been built yet. Both now hold their tongue until there is a ledger to read.
- **Ghost exports are gone.** A factory could sit there claiming to export a part to another factory that had no matching import, or worse, quietly export the wrong amount. There wasn't one cause, there were five, and each of them is fixed:
  - **Two factories could end up with the same ID.** New factory IDs were drawn at random from 0–9,999 with nothing checking whether the number was already in use, so a 60-factory plan had roughly a one-in-six chance of a collision and a 120-factory plan closer to one-in-two. Exports are recorded against the importing factory's ID, so a collision merged two factories into one as far as the dependency system was concerned: one export request covering two different importers, the wrong factory named on the export chip, the supplier under-producing because it only saw one of the two demands, and deleting either factory taking the other's imports with it. IDs are now issued against the plan so they can't collide (and never 0, which everything else reads as "no factory selected"), and a plan loaded with a collision already in it has the clash broken and its exports rebuilt.
  - **An export could be left behind with nothing in it**, which rendered as the importing factory's name under "Exports:" with no parts against it. The cleanup only deleted an emptied entry while it happened to be looking at it.
  - **Importing the same part twice from the same factory understated the export.** Only one export request is ever raised per supplier and part, and the second import overwrote the first's amount rather than adding to it, so importing 300 and 200 Iron Ingots from the same factory told the supplier only 200 were wanted. The amounts are now totalled, duplicates already saved in a plan are merged on load, and switching an import's factory onto a combination you already have clears the item so you pick again instead of silently creating one.
  - **A corrupt import could take the whole plan down with it.** An import pointing at its own factory threw, which aborted the calculation for every factory: a blank planner over one bad row. The row is now dropped with a warning and the calculation carries on.
  - **A plan that was already wrong stayed wrong.** A plan whose figures look current is deliberately not recalculated when it loads, which is what makes switching tabs fast, but it also meant a ghost saved by an older build survived every reload. Loading now checks that the export and import sides of every link agree, and recalculates when they don't. Anyone already affected gets it cleared the next time they open the planner.
- Deleting an import row no longer deletes the other unfinished import rows with it. Rows were matched by their factory and item, and every row you haven't finished filling in looks identical (no factory, no item), so clicking the bin on one of three blank rows removed all three. The row you clicked is now the row that goes.
- Copying a factory no longer briefly claims to export everything the original does. The copy inherited the original's exports even though the importing factories are still buying from the original; the cleanup tore them down afterwards, but only after a recalculation of every factory involved.
- Copying a factory that supplies other factories no longer throws an error during the recalculation that follows: the copy carries the original's dependency requests, and cleaning up such an orphaned request could pull the entry out from under the cleanup scan itself mid-loop.
- Fuel Generators burning Rocket Fuel no longer ask for a fraction more fuel than they need: a 240-generator plant at 240% wanted 2400.002 Rocket Fuel/min instead of 2400, forcing you to hand-override the producing factory to make the two line up. The game data is at fault: the parser worked out the MW-per-item figure by dividing a generator's power by a fuel rate it had *already* rounded to five decimal places, so Rocket Fuel's 4.1666…/min burn rate left MW-per-item at 59.999952 instead of exactly 60. The planner divides a plant's total MW by that number, so the error scaled with the plant: 1,200 generators at 240% drifted by a whole 0.01/min. MW-per-item now comes straight from the fuel's energy value (3,600 MJ ÷ 60s = 60 exactly), and fuel rates are no longer rounded before they're stored. Compacted Coal was off in the same way (10.499996 instead of 10.5) and is fixed with it.
- Alternate: Super-State Computer asked for 7.199999999999999 Computers/min instead of 7.2, and the two Uranium Fuel Rod recipes had the same wobble on Crystal Oscillators and Encased Industrial Beams. The parser was working out rates as `(60 / duration) × amount`, which loses precision on the divide; multiplying first fixes all three.
- Plans you already saved with those wrong numbers baked in now repair themselves when they load. Correcting the game data fixes everything the planner recalculates, but the quantities you *asked* for, such as a product's amount, a generator's fuel or power figure and an import amount, are inputs to that calculation rather than outputs, so they carried the drift forward for good. Loading a plan now sweeps those for quantities sitting a rounding hair off a whole number and snaps them back, then recalculates. The check scales with the value rather than using a flat allowance, because the drift did too: 2400.002 and 12000.01 are both caught, while a quantity you genuinely chose (12000.5, or anything more than one part per million off an integer) is left exactly as you set it. Anything on a building group whose clock you dialled in at a fractional percentage is skipped entirely, because that precision is deliberate. Power generators saved before the fix also carried a stale copy of the old MW-per-item figure; that's refreshed from the current game data at the same time. When anything is repaired you get a dialog listing exactly what changed: which factory, which item, and the figure before and after, so nothing is altered behind your back.

### Release notes for v0.6

- **A "What's new" deck for the release**, in the shape the v0.5 one had: seven slides opening on the breaking change, since that is the thing that changes what every existing plan reports, then mining, the wizard, groups, icons, the rest of the planner work and the fixes. The v0.5 deck is kept exactly as it shipped, no longer opening on its own, and reachable from the last slide for anyone who missed it.
- **A first-time visitor doesn't get it on their first visit.** Whether the introduction has been dismissed is read once when the page loads, so someone dismissing it *now*, which is to say someone seeing the planner for the first time, isn't handed a release deck on top of their first ever look at it. Intro, then the plan they asked for, and nothing else. It isn't marked as seen either, so the deck is waiting for them next time. This is how the v0.5 deck behaved; v0.6 briefly watched for the dismissal instead, which put the two dialogs back to back.
- **The breaking-change warning is shown to the people it can have broken.** A plan created after this release is stamped as answered the moment it exists, so there was nothing for the red **Action needed** banner to warn its owner about, and holding someone on a warning that cannot apply to them is how you teach them that warnings here are noise. Slide 1 now leads with the banner, and locks the deck, only where the plan actually predates the change and is short of something. Everyone else gets an ordinary first slide they can read and close.
- **Where it does apply, the deck cannot be waved away.** Nobody reads a dialog they can close in the corner of their eye, and this is the only warning most people will get that their saved plans now read differently. No cross, no click-outside, no escape, and the Next button stays disabled until the warning is answered; the way out afterwards is the "Got it!" at the far end of the tour. The choice (**Fix my plans with the Raw Resources Wizard** or **I understand, I'll fix my plans myself**) sits directly beneath the banner where the warning is, and again in the footer for anyone who has scrolled past it, since slide 1 is long and a window that won't close needs its answer on screen. Reopening later from **Show changes** has none of this: by then it is reference material, not news.
- **Running the wizard from the deck no longer ends the tour.** It used to close and stay closed, so anyone who took the offer on slide 1 (the people the release most affects) never saw the other six slides. The deck now steps aside for the wizard and comes back where it left off once the wizard is done with, applied or cancelled alike, unlocked because running it *was* the answer.
- **The deck speaks for the raw-resources notice rather than queuing behind it.** Both ship in this release, so a returning user would otherwise have been handed two dialogs saying the same thing on the same load. The notice is only *deferred* while the answer is outstanding, never marked as seen, so a tab closed on that slide gets the warning again on the next load.
- The in-app Change Log's Beta v0.6 section now covers the whole release rather than only the mining half. **Its raw-inputs entry was also wrong**: it described a global switch with per-factory overrides, which was the shape of the feature before the assumption was removed outright. Anyone reading it was being sent to find a setting that does not exist.

### Under the hood

- The rule that exports and imports must agree is now written down once, in `factory-management/dependency-integrity.ts`, and enforced from both ends: the app runs it when a plan loads (which is what triggers the repair recalculation above), and the test suite asserts it after every mutation it makes. Alongside the per-case regression tests there's a seeded fuzz suite that walks the demo plan through sixty random edits (deleting imports and products, retargeting imports, adding, copying and deleting factories) and re-checks the invariants after every single one, so orderings nobody thought to write a test for are covered too. A failure names the seed that reproduces it.
- The cleanup scan that removes invalid export requests was iterating an array that the very cleanup it triggers replaces underneath it, and ran all three of its checks against a request it had just deleted (recalculating both factories again for each). It now iterates a snapshot, skips requests already gone, and stops at the first check that removes one.
- `pnpm test` no longer fails at random after every test has passed. The suite's game-data server on port 3001 was never being shut down: its teardown was wired up as `globalTeardown` in the Vitest config, which is a Jest option. Vitest ignores it silently, and takes its teardown from what `globalSetup` returns instead. The listening socket then kept the process alive, Vitest's `close()` blew its 10s budget ("something prevents Vite server from exiting"), and the run exited 0 or 1 depending on timing. The closer moved into `globalSetup`'s return value, so it actually runs; it also drops live connections first, and skips closing when the port was already occupied and no server of ours was ever started.
- Power generator quantities now opt into the same whole-number snapping that product quantities already use, so any residual hair-off-an-integer arithmetic on the power path is presented as the number it means. As with products, a building group whose clock was dialled in at a fractional percentage is deliberate precision and is never snapped. The predicate that decides this moved out of `products.ts` into `factory-management/common.ts` as `hasFractionalClock`, shared by both paths.
- A new parser test sweeps every number in the generated data file for float noise, meaning a value sitting within a rounding hair of a short decimal without being it, so this class of bug can't reappear unnoticed. Genuine repeating rates (1.6666666666666667 Crystal Oscillators/min) are left alone; they're already the closest representable value.
- The three per-package `pnpm-lock.yaml` files were consolidated into one at the repo root (`sharedWorkspaceLockfile: true`). Renovate only ever collects the workspace-root lockfile as a changed artifact, so with the lockfiles split every `catalog:` bump shipped as a one-file diff to `pnpm-workspace.yaml` and failed all four CI checks on `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`, deterministically, for the seven catalogued packages, with no way to fix it from the hosted Renovate app ([renovate#37485](https://github.com/renovatebot/renovate/issues/37485)). One lockfile also means one resolution per dependency instead of three that can drift: 1,490 package entries across the old files collapse to 1,091, and each package's `node_modules/.pnpm` folds into a single store at the root. The API image now copies the root lockfile plus all three manifests (`--frozen-lockfile` validates every importer in the lockfile, including the ones it won't install) and narrows the install with `--filter backend`; the build workflows gained the same filter so a job stops installing the other two packages' dependencies. `onlyBuiltDependencies` was rewritten as an `allowBuilds` map at the same time, because pnpm 11 removed the old key, so it had quietly been dead config.
- Upgraded Vuetify 3.12 → 4.1, which is the Material 2 → Material 3 rewrite, with the planner's appearance deliberately held still. Five of v4's breaking changes actually reach this app, and each is compensated rather than absorbed: the MD2 `text-*` typography classes are gone (`.text-h1`–`.text-h6` were already redefined in `global.scss`, but only for size and weight, and the letter-spacing came from Vuetify, so it's now stated explicitly, along with `body-1`/`body-2`/`subtitle-1`/`caption`); the global `* { margin: 0; padding: 0 }` reset is gone, which would have handed browser margins back to ~330 raw headings, paragraphs and lists; `VBtn` lost its uppercase and its wider tracking; the grid swapped negative-margin gutters for `gap`, moving column content flush to the row edges; and the breakpoints shrank (md 960→840, lg 1280→1145, xl 1920→1545), which matters because the sidebar is a `d-lg-flex`. The breakpoints are pinned back in Sass rather than only via `display.thresholds`, because that option moves the JS `useDisplay()` values while the media queries behind `.d-*`, `.v-col-*` and `.v-container` are baked into the compiled CSS. So `vite-plugin-vuetify` now builds Vuetify's styles against a settings file (about 6% of build time), which brings the container max-widths back with them.
- The subtle half of that upgrade is cascade layers: v4 puts all of its own CSS in `@layer`, and unlayered rules beat layered ones no matter how specific. Font Awesome's SVG framework injects an unlayered `.svg-inline--fa { font-size: inherit }` at runtime, so every icon in the app silently shrank to its parent's font size, 18px to 14px inside a button. The fix is `font-size: revert-layer` rather than a hardcoded size, so the component rules that outrank `.v-icon--size-*` on specificity (the 28px alert icon) still win. The same reversal caught our own `.fa-bolt` margin, which used to lose to `mr-1`'s `!important` and now beat it. Worth knowing for anything added later: a rule in `global.scss` now takes precedence over Vuetify's by default, which is the opposite of how it behaved.
- Upgraded ESLint 9 → 10 across all three packages. The one blocker was the style layer: `neostandard` (still ESLint 9-only) bundles `@stylistic/eslint-plugin` v2, which calls `SourceCode` methods ESLint 10 deleted. web now depends on `@stylistic` v5 directly and hands it to neostandard's configs in place of the bundled v2, patching the three rule configs v5's schemas no longer accept (`func-call-spacing` → `function-call-spacing`, `object-property-newline`'s `allowMultiplePropertiesPerLine` → `allowAllPropertiesOnSameLine`, and the deprecated JSX aliases this project has no use for). Swapping the plugin object beats a pnpm override, which lives in `pnpm-workspace.yaml` and breaks the install anywhere pnpm doesn't read overrides from there, the Vercel build image included. The whole shim comes out once neostandard supports ESLint 10. `@eslint/js` also moved onto the workspace catalog alongside the other shared eslint packages. Rule behaviour is otherwise unchanged; the only code change is auto-fixed indentation in three TS type annotations.

## Beta v0.5 — The "Overclocked" Update

_Released 21 July 2026._

After a long hiatus: the highly anticipated Overclocking and Somersloop support via Building Groups, a huge Power update, an item-centric Parts & Recipes browser, and a planner-wide UI refresh.

### Building Groups — Overclocking & Somersloops

- Each product and power generator can now be split into Building Groups, letting you plan how production lines are physically laid out in your world. Each group has its own building count and clock speed, and the planner keeps them in sync with the production targets.
- Implemented Somersloops (production amplification) on product building groups: per-building somersloop input clamped to each building's slot count (smelter/constructor 1, assembler/foundry/refinery/converter 2, manufacturer/blender/particle accelerator/quantum encoder 4; packager and generators cannot be amplified). Output (product + byproducts) is boosted by `1 + filled/slots`, power by `(1 + filled/slots)²`, both stacking with overclocking per the wiki formulas (fully slooped at 250% ≈ 13.43× base power). Ingredient consumption is never amplified — the item's ingredient demand is discounted to what the machines actually consume, and effective/remaining buildings, rebalancing, part reverse-solving and remainder actions are all somersloop-aware. Covered by a new unit suite (`building-groups/somersloops.spec.ts`) and TDD suite (`tdd/building-groups/somersloops.spec.ts`, refs `BG-E-S-PROD`).
- The "Open/Close Building Groups" bar shows the total Somersloops (products) and Power Shards (products + power producers) the item's groups consume, always visible with 0 when unused — both resources are rare, so their usage needs to be visible at a glance. Shards follow the game rule of 1 per building per 50% clock above 100% (150% = 1, 200% = 2, 250% = 3).
- Reworked how Building Groups are opened: the toggle moved from the cramped top-right control strip to a full-width "Open/Close Building Groups" bar underneath each product and power producer (chevron up when closed, down when open), which expands the groups as a tray below it (red with a warning when the groups have a problem). Inside the tray, the action buttons (Evenly balance, Remainder to last/new group, OC @ 100%) sit at the very top, followed by the status/sync row, then the groups, with "Add Building Group" centered at the bottom.
- Rewrote the Building Groups intro tooltip (shown on the closed toggle) to actually explain what Building Groups do, and shortened the Sync status tooltip (which also wrongly claimed sync re-enables when groups are reduced to 1 — it stays off).
- The "Open Building Groups" bar is disabled while the product has no item/recipe selected.
- Implemented `isEvenlyBalanced` logic for Product Groups to improve user feedback and action button states.
- Added comprehensive TDD tests for Building Groups: Creation, Deletion, Action Buttons, and Item Editing.

### The Power Update

- Added Geothermal Generators and Alien Power Augmenters as selectable power generators. Geothermal is fuel-less — you pick the geyser's node purity (Impure 100 MW / Normal 200 MW / Pure 400 MW average) and the planner shows the 0.5×–1.5× oscillation range; it cannot be overclocked and has no building groups (they'd only echo the building count). The Alien Power Augmenter produces 500 MW static per building and applies a grid-wide circuit boost — +10% of the plan's total generation per augmenter, or +30% when a building group is toggled to "Supply Matrixes", which also adds real Alien Power Matrix demand (5/min per augmenter) to the factory's satisfaction ledger so shortages surface like any other part. The boost (assuming one power grid, as most players run) is shown on the factory's Power & Buildings card and in the world power statistics, where generation now includes the boost to match the in-game power graph. Both buildings are emitted by the parser from the game docs (data regenerated as v1.2-03) rather than hardcoded.
- Reworked the world Power statistics from chips into a table that makes the circuit boost math explicit: base Generation, Circuit boost (own colour, with %), Total generation, Consumption (average, with the min–max swing range when variable-power buildings are present) and Difference. An advisory note recommends covering at least the average draw and absorbing spikes with Power Storage rather than generating for the peak. Alien Power Augmenter building groups are always kept in sync (there is nothing to fine-tune without clocks or fuel), and their tray hides the balancing actions and sync/effective-buildings status — only the groups themselves and "Add Building Group" remain.
- The world Power statistics table has Average / Minimum / Maximum columns: generation carries the variable-generator swing (Geothermal), the circuit boost scales with it, consumption shows its existing range, and the difference takes the averages with an asterisked footnote advising Power Storage coverage up to the maximum consumption.
- The power target is now plan-specific: it lives on the factory tab (travelling with save/load/share) instead of a single browser-wide value, with the old global value used as a fallback until the target is next changed.

### Parts & Recipes

- Revamped the Recipes page into an item-centric "Parts & Recipes" browser at `/parts` ([#224](https://github.com/satisfactory-factories/application/issues/224)); `/recipes` redirects there. Each part is an accordion showing the recipes that produce it (with amounts per minute, byproducts, and the building used with its power draw — previously recipes showed no products or building at all), alternate recipes tucked into a nested "Alternate Recipes" accordion, and a "Used in" accordion listing every recipe that consumes the part. Search is now fuzzy across both part and recipe names, with results split into Parts and Recipes sections, and every recipe card has an "Add to Planner" button that opens a dialog listing your factories — add the recipe to any of them (or a new factory) at the default rate of 1 building @ 100% clock, with a "Go to Planner" shortcut. The dialog highlights factories that already produce ("Produces this") or consume ("Uses this") the part. Parts produced by a factory in the current plan get an "In Plan - X/min" badge (totalling production across the plan), a "Produced in Plan" filter chip narrows the list to those parts, and the opened panel lists Exports-style factory chips (with per-factory /min) that jump straight to that factory in the Planner. Search is debounced and subsequence matching is constrained (word-boundary start + compactness) so e.g. "iron" no longer matches "Packaged Nitrogen Gas".
- Recipe cards now lay out in a responsive grid (three across on desktop, two on medium screens, one on mobile) instead of stacking full-width, and "Produced by" became its own accordion (open by default) alongside "Alternate Recipes" and "Used in". The accordions got a greyer background, light border and shadow so they stand out from the part panel, recipe cards inside are darker for contrast, and part entries in the list are now slightly lighter than the page background.

### UI Refresh

- Unified the planner's colours into a single source of truth (`web/src/utils/colors.ts`): semantic tokens (product, byproduct, raw resource, building, import, factory, somersloop, power consumption/generation, circuit boost, peak consumption, success, error, problem) published as `--sf-*` CSS variables at boot and consumed by the `sf-chip` classes, scoped styles and Vuetify props alike. This fixes long-standing drift — power consumption was one colour in the statistics table and another on chips, "problem red" existed as four different literals, and Vuetify's `color="error"` resolved to Material pink instead of the app's red (the theme now maps `error`/`success` to the tokens). The problem background is now opaque, so a problem factory renders the exact same red in the sidebar, card header and summary table (the old 40%-alpha value composited differently on each surface). New "factory" token (white on grey, sharing the card header's background) is used everywhere another factory is referenced: satisfaction export chips, summary rows and the collapsed view.
- Power chips now follow one visual language everywhere: any bolt + minus (consumed power — the product "Requires" chip, the collapsed Statistics "Consumed" chip, Power & Buildings "Consumes", building-group under-chip figures and the recipe card's power chip) uses the power-consumption colour instead of yellow, and positive generation (generator output, power building groups, "Produces"/"Generated") is green. The factory header's net power chip shows the sign as a plus/minus icon next to the bolt (green when net-generating) rather than a sign baked into the number, the header's Power Shard chip now sits before the Somersloop chip, and the header chips actually have spacing between them (a `no-margin` class was silently cancelling their margins). The Game Sync info tooltip moved inside the sync chip (left of the reset button) instead of floating detached after it.
- The sidebar now has Statistics and Factories Summary entries above the factory list: both jump-scroll to their section, and the Statistics entry shows compact power chips (generated, consumed and difference, in GW) with the entry flagged problem-red when the plan runs a power deficit.
- New "Power Shards & Somersloops" statistics section (above the Building Summary): two centred tables — Power Shards first, then Somersloops — listing each factory that uses them (factory chip, click to jump) with per-factory amounts and a Total row. Somersloop counts include Alien Power Augmenter construction costs (10 per augmenter).
- The statistics section headers now carry at-a-glance chips visible even when a section is collapsed: total generation/consumption/difference on Power, resource count on Raw Resources, surplus/deficit counts on Product Surplus & Deficit, shard/somersloop totals, total building count on Building Summary, and a factory count on the Factories Summary (echoed in the sidebar jump-link). All section Show/Hide buttons are right-aligned to match the main card headers.
- Factories Summary facelift: the table is now an at-a-glance view built around item icons instead of a full info dump. The "Items" column is renamed "Products", and product/building chips show just the icon with the per-minute rate (or building count) — hover a chip for the full name, amount and any surplus/shortage. A new "Satisfaction" column shows a green "Satisfied" chip or red per-part shortage chips. Import and Export chips now list the source/destination factory on lines inside the chip (click one to jump to that factory). Also fixed the summary table cells missing their bottom padding, the summary's chips being vertically crushed (Vuetify pins chips to a fixed height, so their vertical padding never applied — summary chips now grow to fit), and the summary starting hidden on first visit due to a string-to-boolean bug.
- Products and Satisfaction columns in the Factories Summary are now fixed-width, wrapping their chips onto new lines instead of stretching the table.
- Reworked the collapsed ("hidden") factory view: imports and exports are grouped into factory-coloured chips containing the part chips (click to jump to that factory), raw resources get their own group with cyan part chips, import parts are grey, export/producing parts are product blue, and all chips share one consistent height (producing chips previously rendered taller than the rest).
- Factory card headers now show a net power chip (generation minus consumption, with a generated/consumed breakdown tooltip) plus Somersloop and Power Shard totals (shown only when used), matching the height of the existing tasks/notes/sync chips.
- Chip spacing is now owned by flex gaps instead of per-chip margins in the factory header bar, the Power statistics summary row, and the collapsed Statistics card — previously the first chip carried a different gutter (12px) than the rest (8px), the sync chip had none at all, and part chips inside the collapsed view's factory pills (e.g. Limestone/Water under Raw Resources) touched each other because the global `no-margin` rule silently cancelled their margin utilities.
- The app chrome went FICSIT orange: the main header is now burnt orange with a gold bottom border and a drop-shadowed, properly inset logo, the tab bar beneath it is a neutral dark grey whose bottom border matches the sidebar divider, and the beta badge swapped its clashing blue for a yellow-tinted treatment that sits on the orange. Active tabs — both the header routes and the factory tab bar — underline in the consumption orange (`--sf-power-consumption`), which the tab bar's controls (Hide Sidebar fill, bare pencil/+ glyphs) share. The Discord button gained a visible indigo outline in place of Vuetify's hairline `border`.
- In the collapsed factory view, parts imported from other factories now use product blue (they were wrongly grey), and the part icons inside the import/raw-resource/export pills are sized to fit their chips instead of clipping at the top and bottom.
- The collapsed factory view now lays out Imports / Producing / Exports as a table: a fixed-width right-aligned label column with the chips beside it, so every section's chips start at the same x, labels stay vertically centred when chips wrap to multiple lines, and the previously asymmetric section padding (8px top / 4px bottom) is an even 8px all round. Producing chips are full-size, matching the height of the import/export factory pills.

### Performance — the reactivity overhaul

- Massively reduced the reactive cost of recalculations. The calculation engine used to rebuild parts / metrics dictionaries directly on the live (reactive) plan on every pass — ~98% of those writes left values unchanged, yet each one still fired deep watchers (localStorage persistence, the sidebar factory list, and, catastrophically, Vue Devtools' sync store subscription, which deep-traverses the entire plan per write and made adding a factory hang for minutes on large plans). `calculateFactory` / `calculateFactories` now run the engine on a plain `structuredClone` of the plan and diff-commit only genuine changes back onto the live objects, preserving object identity throughout. A no-op recalculation now performs zero reactive writes (verified down from ~2,400 on the demo plan), a product edit fires ~30 watcher events instead of thousands, and `factoryUpdated` events are only emitted for factories whose data actually changed. Also fixed `setFactories` aliasing `previousInputs` to the live `inputs` array instead of snapshotting a copy.
- Added a browser-level reactivity test harness (`web/testing/browser/`) that drives the real planner (demo plan and Mael's MegaPlan) with Puppeteer, asserting DOM reactivity end-to-end and bounding the number of deep-watcher fires per interaction via a dev-only `window.__sfWatchCounter` hook.
- Added a 124-factory stress-test plan (4× Mael's MegaPlan with remapped ids, `factory-setups/stress-plan.ts`, loadable in dev via `window.__sfLoadStressPlan`) plus a browser stress harness (`stress.e2e.mjs`) that expands every factory, materializes ~294k DOM nodes, and measures edit→DOM-settle latency, DOM mutations, watcher fires and main-thread long tasks per edit. It confirms calculation churn is solved at scale (~37 fires, ~65 DOM mutations per edit; a no-op recalc of all 124 factories is 0 reactive writes / ~240ms) and pinpointed the next bottleneck: full-plan deep watchers re-traversing the whole plan per flush plus the persistence `JSON.stringify`, blocking edits ~4–6.5s on a 124-factory plan.
- Eliminated every full-plan deep watcher. Plan persistence is now event-driven instead of deep-watched: calculation commits (`factoryUpdated` / `calculationsCompleted`) trigger a debounced save that stringifies the raw tree (`toRaw` — several times faster than stringifying through proxies), explicit store mutations schedule saves directly, and direct mutations that bypass the calculator (renames, hidden toggles, tasks) are caught by a periodic compare-and-save plus a flush on `pagehide`/tab-hide — verified in the browser harness by reloading and asserting both a calculated edit and a non-calc factory rename survive. The sidebar factory list now watches an id fingerprint instead of deep-watching the plan, and the factories summary watches row count. Profiled result: Vue's deep-watch `traverse` no longer appears in edit profiles at all; edit→DOM-settle on the 124-factory stress plan dropped from ~5–6.5s to ~2.5–3.5s (measurement overhead included), with the remainder being component-update sweep and native style/layout across the ~294k-node DOM — the rendering rework's territory.
- Reworked input responsiveness around the faster engine. Numeric inputs now split "echo" from "recalculate": the typed value lands in the data instantly, and only the whole-plan recalculation is debounced — at 250ms (down from 750ms), with a consistent spinner beside every debounced input (product amounts, ingredients, byproducts, building counts, power producers, import quantities and building groups) so the pending recalc is visible. A/B-measured in-browser: raw no-debounce edits settle in ~90–180ms on the demo plan but ~580ms/keystroke on a 124-factory plan, and — decisively — without a debounce the recalc rewrites derived inputs (e.g. Requires quantities) mid-typing, corrupting entered values; the 250ms window is the guard against that (typing cadences ≤200ms/key are safe). A dev-only `window.__sfDebounceMs` override supports future A/B tests. Follow-up idea: suppress write-back to focused inputs, which would allow dropping the debounce entirely.
- Adding or removing a factory now persists and announces immediately (`factoryUpdated` + scheduled save) instead of waiting for the periodic safety net — previously a freshly added factory wasn't saved (or seen by sync) until up to 10 seconds later. The persistence safety-net interval is now 5s, and a dev-only `window.__sfLoadsCompleted` counter exposes the `loadingCompleted` event to browser tests.

### Quality of Life

- The factory list sidebar can now be resized by dragging its edge, or collapsed entirely to give the plan the full width of the screen — with a nudge shown when it is first hidden.
- Shortages in the Satisfaction table now offer two new buttons under the shortage chip: "+ New" creates a brand new factory producing the missing amount, and "+ Existing" opens a dialog to pick one of your existing factories (highlighting ones that already produce the part). Both wire up the import back into the shortage factory so the deficit is actually resolved, then jump you to the supplying factory — a "Jump to factory after adding" checkbox in the dialog (remembered in localStorage) turns the jump off. The buttons show an "Adding..." spinner state while the plan recalculates.
- The demo plan gained two factories: "Alien Power" (three Alien Power Augmenters — two supplied with Alien Power Matrixes for +30% grid boost each, one dry at +10%, with a deliberate matrix shortage showing fuel demand on the satisfaction ledger) and "Geothermal Power" (one producer per geyser purity: 4 Impure, 3 Normal, 2 Pure = 1,800 MW average, 900–2,700 MW swing). Mael's MegaPlan template gained the same two factories (its augmenters run unfueled — the plan only spares 4/min matrixes and a fueled augmenter needs 5).

### Fixes

- Quantities no longer come back a rounding-hair off what you typed (the reverse-solve rounding artifact). Typing a whole-number ingredient quantity (e.g. 1234 Crude Oil for Plastic) reverse-solves the product amount, which is often a repeating decimal (822.666…); capped at the game's 3-decimal precision, the recomputed ingredient then displayed as 1234.001 (or 1233.999 from older under-rounded data). Since exact round-trips are mathematically impossible with both sides quantised at 3dp, quantities within 0.002 of a whole number now snap to it (`snapNearInteger`) — at that precision it is vanishingly unlikely the user wanted 120.001 rather than 120. The snap is context-aware via a new `clockSetByUser` flag on building groups: when the user has dialled in a fractional overclock themselves (e.g. 223.333%), that precision is deliberate and derived quantities are kept exact — a converter at that clock still shows the game-accurate 535.999 Stone — whereas solver-derived fractional clocks (a typed quantity re-solving to 42 buildings @ 97.9365%) don't suppress the snap. It also never collapses tiny legitimate quantities (0.001 of a byproduct) to zero. Covered by unit tests on the snap math, engine round-trips and the user-clock exemption, plus a browser test typing into a real Requires input.
- Switching factory tabs no longer recalculates the whole plan. `initFactories` used to unconditionally re-run the calculation engine on every load, even though a tab's stored data is already fully calculated — on large plans this blanked the screen and hung for 4–5 seconds every time you changed tabs. Recalculation now only runs when a data migration actually backfilled a missing field (old saves); an already-current plan loads instantly. Also fixed a listener leak in `BuildingGroups.vue`, which registered a global `factoryUpdated` handler on mount but never removed it on unmount — leaked handlers accumulated across tab switches and multiplied the cost of every recalculation.
- Loading a plan template no longer zeroes out its fuel-based power generation. Producers defined only by a power amount (as plan templates are) got their default building group before any calculation had derived a building count, leaving a 0-building group — which the "building groups are sacrosanct" recalculation then faithfully synced the producer down to. A group set totalling zero buildings while its item has real ones is now treated as degenerate and rebuilt from the item, and the load-time migration no longer backfills groups for uncalculated producers. This also self-heals previously saved plans carrying such zero-groups.
- Fixed variable-power buildings (Particle Accelerator, Converter, Quantum Encoder) consuming almost no power in the planner (e.g. a Nuclear Pasta accelerator at 250% with full Somersloops showed 1.3 MW instead of ~13.4 GW). Two bugs compounded: the building-group power calculation read the flat buildings map, which only holds these buildings' 0.1 MW standby draw, instead of the recipe's real power; and the parser treated `mVariablePowerConsumptionFactor` as the maximum draw when it is actually the range *on top of* the constant (so Nuclear Pasta emitted 500–1000 MW avg 750 instead of the correct 500–1500 MW avg 1000). The parser formula is fixed (game data regenerated as v1.2-02), recipes now carry `minPower`/`maxPower`, and the planner calculates with the average while displaying the oscillation range — building group rows and product power chips show "avg (min – max)", and the factory Power & Buildings card plus the world power statistics show the peak draw alongside the average so grids can be sized against tripping. World power statistics now also sum the group-derived per-factory figures (accounting for overclocks and Somersloops) instead of the 100%-clock building requirements.
- Building groups are now sacrosanct during recalculation: the Recalculate action and plan loading never rebalance groups (counts, clocks, somersloops, matrix toggles) — when an item disagrees with its groups, the item's quantity is adjusted to match the groups instead, never the other way around. Regular item edits still rebalance groups as before (the sync feature), but an edit that doesn't change the effective building requirement now leaves a custom group split untouched. Guarded by a dedicated test suite (`building-groups/recalculate.spec.ts`).
- Editing a building group's output value no longer blows away a hand-set overclock. Previously, entering (or re-entering) an output on a group that was already overclocked would re-solve the whole group — e.g. 1 Converter @ 223.333% became 3 Converters @ 75% — because the solver preferred spreading the work across more buildings at a sub-100% clock. When an overclock is set, the group's building count is now preserved and only the clock is rescaled to hit the new output (falling back to a full re-solve only if that would exceed the game's 250% cap).
- Fixed a synced product's qty/min not matching its building groups to 3 decimal places. The item quantity was derived from the effective building count rounded to 4dp (e.g. 1 Converter @ 223.333% with a somersloop rounded to 3.35, giving 3.35 × 120 = 402), while the group produced 401.999; the quantity is now computed from the full-precision effective building count so it matches the group output exactly. A group's ingredient parts are likewise now computed straight from the recipe rate and the group's own clock rather than round-tripping through the item's amount-derived requirements, so they no longer drift by 0.001 when the item quantity changes. Covered by a Converter "solidification" spot-check (223.333% + one somersloop → 22.333 Reanimated SAM, 535.999 Limestone, 401.999 Iron Ore).
- Fixed the effective building count rounding each group to 3 decimal places when clocks support 4 (e.g. one Converter at 223.33% produced 267.96/min instead of the in-game-correct 267.996/min). Caught by a new Converter spot-check suite verified against in-game values, covering ingredients, Somersloop-amplified output and the variable power range at 250% with/without Somersloops and at 223.33%.
- Fixed editing a building group's building count updating the product's amount but not its ingredient requirements (or the factory's parts/power) until some other edit forced a recalculation. The calculation passes ran against the pre-writeback amounts and the gap was previously masked by a Vuetify labs `VNumberInput` quirk that spuriously triggered a second recalculation from the UI. `calculateFactory` now re-runs itself once when a building-group sync actually changes item amounts.
- Fixed the "Building Groups have a problem!" state falsely triggering when editing a group with sync ON (e.g. adding a Somersloop): the problem flag was computed before the group→item writeback and never refreshed, so a single calculation pass (what the UI runs) left it stuck red against stale item data. It is now recomputed after the writeback.
- Fixed group→item writeback (`checkForItemUpdate`) running on every recalculation, which replaced exact user-entered amounts with float-degraded values (e.g. 123 → 122.999…) and stomped the power producer's `updated` direction. It now only runs for edits originating from a building group.
- Fixed critical bug where `syncBuildingGroups` ignored the `buildingGroupItemSync` flag, causing unwanted rebalances.
- Fixed `deleteBuildingGroup` logic to maintain sync disabled after multiple groups are reduced to one, ensuring intentional imbalances are preserved.
- Fixed `BuildingGroups.vue` to ensure metrics are recalculated immediately on mount and on all relevant factory updates.
- Corrected default building count for newly added building groups to match current UI design (default 1).
- Updated "Evenly balance" button to work even when sync is disabled, providing better manual control.
- Fixed power producer building groups being excluded from the factory's total building requirements.
- Fixed `addPowerProducerToFactory` dropping the `ingredientAmount` option — it now seeds the fuel rate, matching the syncState mapping.
- Fixed the product building count input displaying raw float noise (e.g. 1.3333333333333333); it now displays at 3dp while keeping the underlying value precise.
- Product building count edits with negative values are now clamped to 1.
- `getPowerRecipe` no longer crashes on an unknown recipe id.
- Items from old saves without a `buildingGroups` field no longer crash the parts calculation before migration runs.
- Items that no recipe can produce (Leaves, Wood, Mycelia, alien remains, power slugs, SAM, FICSMAS Gift) are no longer selectable as products ([#390](https://github.com/satisfactory-factories/application/issues/390)). The parser now strips any part that isn't the product of a production recipe or a power-generation byproduct from `items.parts` (guarded by tests), and the game data was regenerated as v1.2-01. These items remain available as raw resources and biomass burner fuel.
- Fixed the Statistics section starting hidden for first-time visitors (`Boolean('false')` is `true` — the same string-to-boolean bug previously fixed for the Factories Summary).
- The per-factory Satisfaction section now renders its Power & Buildings card for factories that only contain fuel-less power generators (previously it required at least one part to exist, hiding the power figures entirely).
- The collapsed Statistics strip's "Generated" chip now uses the same grey generation colour as the power table and sidebar, instead of yellow.
- Unpackaged liquids that are also a Raw Ingredient (e.g. Crude Oil / Water) are no longer counted twice as both Raw Import and Production ([#431](https://github.com/satisfactory-factories/application/issues/431)), and are represented on the Satisfaction section via an "Unpackaged" badge.
- Byproducts (e.g. Water produced as a byproduct of Aluminum Scrap) are now considered "Recycled" into the system: the planner no longer incorrectly requests additional imports for them, and such items are marked with a "Recycled" badge on the Satisfaction section ([#243](https://github.com/satisfactory-factories/application/issues/243)).
- It is no longer possible to enter more than .001 of precision into an item's quantity field, as the game doesn't operate any lower than that; fixed a few rounding error bugs in the process ([#54](https://github.com/satisfactory-factories/application/issues/54)).
- Deleting a factory that imported from other factories no longer displays false "corrupted data" error messages for those factories ([#398](https://github.com/satisfactory-factories/application/issues/398)).
- Fixed several test-only issues related to stale DOM elements and floating point precision.

### Under the hood

- Migrated the repository to a pnpm workspace (per-package lockfiles) and moved to Node 24; Node 26 is also supported now that jsonwebtoken 9.0.3 dropped its transitive `SlowBuffer` dependency.
- Modernised the routing/plugin stack: vue-router 4 → 5 (the deprecated `unplugin-vue-router` was merged into vue-router core, so the separate package is gone and the Vite plugin now comes from `vue-router/vite`), the unmaintained `vite-plugin-vue-layouts` is replaced by its community successor `vite-plugin-vue-layouts-next`, and the unplugin trio jumped to their new versioning scheme (auto-import 21, vue-components 32, fonts 2 — ESM-only, config unchanged). Pinia 2 → 4 with `@pinia/testing` 2 (stores were already setup-style, so no code changes). Verified end-to-end in a real browser: routes, the `/recipes` → `/parts` redirect, layouts, and plan persistence across navigation all work with no console errors.
- Upgraded TypeScript 5.9 → 6.0 (the final JS-based line, semantically aligned with the new Go-native TS 7) and vue-tsc 2 → 3. TS 6 turns pre-TS-7 deprecations into errors, which drove some genuine cleanup: removed the deprecated `baseUrl` from tsconfigs (paths are anchored with `./` now; parsing's alias config was dead and simply deleted), moved the backend to `module`/`moduleResolution` `nodenext`, removed the long-warned-about `defineProps`/`withDefaults`/`defineEmits` compiler-macro imports from 11 components, and deleted two dead template refs vue-tsc 3 now correctly flags as unused. TS 7 itself waits until vue-tsc/typescript-eslint/ts-jest support its new API (expected after TS 7.1).
- Migrated linting to ESLint 9 flat config across all three packages. The dead `eslint-config-standard` / `eslint-plugin-node` / `eslint-config-vuetify` v1 stack is replaced by `neostandard` (standard's flat-config successor) plus `eslint-plugin-vue` 10 and `@vue/eslint-config-typescript` 14 in web, and `typescript-eslint` + `eslint-plugin-import-x` in backend/parsing. Rule behaviour is preserved (the v1 vuetify config's style rules were ported verbatim); the only code changes are auto-fixed spacing in TS type literals and two newly-caught nits (an unused catch binding and a ternary-as-statement).
- Upgraded the web build/test toolchain across two majors: Vite 5 → 7, Vitest 2 → 4 (plus @vitejs/plugin-vue 6, coverage-v8 4, jsdom 29, vite-plugin-vue-devtools 8, @types/node 24). This clears **all 37 known vulnerabilities** in the web package, including a critical arbitrary-file-read/execute hole in Vitest's UI server. Test-suite adaptations for Vitest's new mock semantics (`resetAllMocks` now restores real implementations and `spyOn` reuses existing mocks, so a few tests needed their spy history cleared explicitly).
- Upgraded every dependency across web, backend and parsing to its latest patch/minor version (highlights: Vuetify 3.7 → 3.12, Vue 3.5.40, Vue Flow 1.48, sass 1.101, Mongoose 8.24, Express 4.22, jsonwebtoken 9.0.3 — which fixes the backend crashing on Node 26, so Node 26 is now supported). Vuetify's `VNumberInput` graduated from labs along the way with two behaviour changes that needed adapting: it now rounds typed values to whole numbers by default (the planner configures unrestricted decimal precision globally, keeping fractional per-minute rates working), and it mis-handles the `v-model.number` modifier (silently dropping every keystroke), so the redundant `.number` modifiers were removed from all number inputs. (vue-router was held at 4.4.x during this pass, then upgraded to 5 in the routing-stack modernisation above.)
- `pnpm test` no longer runs Vitest in watch mode (it never finished in CI), and tests run with an in-memory `Storage` implementation under Node 24+.

## Alpha v0.4

_Released 25 January 2025._

A stability release: long-running planner bugs squashed (over **20** of them), measures added to stop the planner breaking in the first place, and a large batch of quality-of-life work across Products, Imports and Satisfaction. [Release video](https://www.youtube.com/watch?v=xiE7AwfzOpc) · [itemised list of changes on GitHub](https://github.com/orgs/satisfactory-factories/projects/2/views/1?filterQuery=+milestone%3A%22Alpha+4%22+&sortedBy%5Bdirection%5D=asc&sortedBy%5BcolumnId%5D=Title).

### Export Calculator v2

- 🆕 The export calculator returns, and now accounts for multiple transport types: trains (solids & fluids), drones, trucks and tractors (solids only). Attempting to transport a fluid by a solids-only method tells you to package it first.
- 🆕 Timer buttons on the "drivable" transport methods: ride the route, start the timer, stop it, and the calculator updates itself from what you drove.
- 🆕 Each transport method shows its accuracy, listing the full assumptions and potential caveats behind the figure.

### Loading Sequence

- 🆕 Loading a plan is completely redesigned. Factories load in sequence, validated as they go to prevent plan corruption, with a progress indicator saying how far along it is. A large plan used to simply appear frozen for 30 seconds or more; now there is feedback at every step.

### Products

- 🆕 Product **ingredients** and **buildings** can have their values changed directly, so adjusting the Wire on a Cable product scales the Cable output to match.
- 👍 Internal byproducts are accounted for properly: a product that also arrives as a byproduct of the same type (Sulphuric Acid as both a product and a byproduct of Encased Uranium Cells) no longer offers a TRIM button.
- 🔧 The "FIX PRODUCTION" double buttons are gone, replaced by proper **Satisfy / Trim** buttons that take more scenarios into account, including internal requirements and exports.

### Imports

- 👍 **Import candidate selection is rewritten.** It now weighs the destination factory's requirements far more strongly, byproducts included. The previous system had a number of edge cases where it would show factories that were not relevant (Water imports appearing for no reason) or fail to show ones that were.
- 👍🔧 **TRIM and SATISFY account for other imports of the same part.** They used to check only the import being trimmed or satisfied against the factory's shortage or overflow; they now calculate the difference across every import of that part.
- 🔧 A factory is no longer an import candidate if it imports a raw resource the destination factory needs.
- 🆕 Redundant imports — where one import alone can cover the demand — are flagged **Redundant!** rather than forcing you to rebalance them.
- 🆕 Imports with no amount set are flagged **No amount set!**.
- 🔧 The TRIM button no longer shows when an import is also a byproduct in the factory.

### Satisfaction

- 👍 **Uranium / Plutonium waste handling.** Producing waste previously required working out for yourself that you needed a power plant, and it was erroneously possible to select Uranium Waste as a product with no recipe. The Satisfaction section now carries a **+ Generator** button that adds a Nuclear Reactor with the correct fuel rod, at exactly the amount needed to produce that much waste.
- 👍 **Part roles.** Each part now shows what role it plays in the factory — Product, Byproduct, Import or Internal — making its relationship to the factory much easier to read.
- 👍 **"MANUALLY FIX" indicators.** Where an automatic fix might not be what you wanted (multiple products, imports or byproducts of the same part), the planner now says so and asks you to fix it by hand rather than guessing.
- 🔧 **TRIM / SATISFY fixes.** A long tail of edge cases resolved, accounting for byproducts and imports across the factory, so the buttons appear only when they apply — which also fixes them doing nothing when pressed.
- 🔧 **Generator power & building maths.** Multiple generator groups of the same building and fuel type were not producing correct power or building counts.

### Fixes & minor adjustments

- 🔧 Fixed a **planner-breaking bug** in how Chrome, Edge and other Chromium derivatives render the mobile tray, which broke the planner outright in those browsers.
- 👍 GameSync now drops factories out of sync when their power generators change.
- 👍 **Tab memory**: changing tabs and reloading remembers the last opened tab instead of always opening the first.
- 👍 **Performance improvements**: the hidden factories UI is now genuinely hidden when a factory is expanded, changes written to a factory are reduced so the UI framework has less to do, and various bits of dead code slowing factory calculation down were removed.
- 👍 The "too many factories open" banner can be dismissed until the page is reloaded.
- 🔧 The template manager can open multiple templates without needing a page reload.
- 🔧 Packaged Rocket Fuel shows the correct icon.
